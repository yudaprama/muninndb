# Hard invariants

Testable assertions a PR must never violate. Anchors are on `develop`. When an anchor's
line number drifts, the symbol name is the source of truth — re-locate by grep, don't
trust the number. If the live code disagrees with an assertion here, the code wins: flag
the stale invariant, don't enforce it.

Format: **[INV-n]** assertion — `file:anchor` — *why it matters / what breaks if violated.*

---

## Cognition invariants

- **[COG-1]** Unknown preset name falls back to `default`, never errors — `internal/auth/plasticity.go` `ResolvePlasticity`. *Config can never wedge the engine.*
- **[COG-2]** All plasticity overrides are clamped, never rejected (HopDepth 0–8, weights 0–1, ACTRDecay 0.01–2.0, HebScale 0–50, PASMax 0–10, **AssocHalfLifeDays 0.5–3650**) — `plasticity.go`. *Out-of-range config is impossible, not merely discouraged.* For `assoc_half_life_days`, `0` means **unset** (fall through to the preset), not "clamp to the minimum" — pinned by `TestResolvePlasticity_HalfLifeClamped`.
- **[COG-3]** The `working` preset equals `default` plus exactly `{RetentionDays:7, BehaviorMode:selective}` — pinned by `TestResolvePlasticity_WorkingEqualsDefaultPlusTwoDeltas` (`plasticity_test.go`). *Editing `default` must not silently drift `working`.*
- **[COG-4]** All presets share `RecallMode="balanced"`, `ArchiveThreshold=0.05`, `InlineEnrichment="caller_preferred"`, `EnrichmentEnabled=true`, `MaxEngrams=0`, `ScoringFusion=""` — `plasticity.go` preset table + the preset-invariant test suites. *These are cross-preset guarantees other code relies on.*
- **[COG-5]** The ACT-R content gate is `contentMatch = w.SemanticSimilarity*vectorScore + w.FullTextRelevance*normalizedFTS`, where `w` is the **resolved** weights (`resolvedWeights` — from the request/plasticity, falling back to `DefaultWeights{Semantic:0.35, FullText:0.25}` only when `req.Weights == nil`) — `internal/engine/activation/engine.go` `computeACTR` + `resolveWeights`. *The content gate is weight-driven, not a fixed constant. When no weights are supplied, the 0.35/0.25 defaults apply and unused components' budget is redistributed (the `1/0.65` scale). A change to `DefaultWeights` or to the resolve/redistribute logic reshapes scoring for every request that doesn't pass explicit weights — verify against the live `resolveWeights` before asserting what the effective weights are.* **Amendment (S1):** an explicit filter defines the candidate SET; the relevance threshold only RANKS within it. Two mechanisms enforce this for candidates that matched an explicit tag filter (`inTagPool`, verified by `passesMetaFilter`), both in `internal/engine/activation/engine.go`: (1) a content-match floor (`tagMatchFloor`) in `computeACTR` gives tag-only hits a sensible positive ordering below genuine content matches; (2) a **threshold bypass** at every Phase-6 scoring cut-site (RRF, CGDN, ACT-R, and legacy weighted-sum paths) — `if final < req.Threshold && !inTagPool` — so an explicit filter match is never dropped for scoring below the relevance threshold. The floor ALONE is insufficient: a floored, content-unrelated, decayed tag hit scores ≈`0.1 * softplus(B~0.17) ≈ 0.017`, below both the ACT-R default threshold (0.1) and the MCP surface default (0.5), so without the bypass every such candidate was still silently dropped. Before this, a content-unrelated tag hit produced `contentMatch = 0` under default (ACT-R) scoring, which zeroed `final` and dropped a candidate the caller explicitly asked for via `tags_all`/`tags_any`/`tag_prefix` — breaking `due:<=today` reminders whose content is unrelated to the query. See `computeACTR`, the four cut-sites in Phase-6 scoring, and `TestComputeACTR_TagPoolFloor_*` / `TestRawTagRange_SeedsDueItems_DefaultScoring` (which asserts 12/12 due items surface at BOTH production default thresholds). The bypass is safe at every site because each is guarded by `passesMetaFilter` first — only candidates that genuinely match the filter bypass the threshold. Non-tag candidates are still thresholded normally.
- **[COG-6]** The effective default threshold is mode-aware and decided in exactly one place per path, keyed on the **effective scoring mode**, not vault config: the `activateCore` coerce runs AFTER the weights block and tests `actReq.Weights.UseRRFFusion` — for rrf-scored requests it leaves `Threshold` at `0` ("unset") so `activation.Run()` applies rrf → 0.001 (#590); otherwise `activateCore` applies the fusion-matched default via `cog6DefaultThreshold` (0.1 for ACT-R, 0.5 for weighted_sum since #754). Keying on the effective scoring mode (rather than `resolved.ScoringFusion`) keeps the threshold default consistent with the scoring math that actually runs even when a caller passes explicit weights on an rrf vault (vault config says rrf, but caller-weights requests score ACT-R and must get the 0.1 default) — `internal/engine/engine.go` `activateCore`, `internal/engine/activation/engine.go` `Run`, `internal/mcp/handlers.go` `handleRecall`. An explicit caller threshold is never modified at any layer. Pinned at the **engine** layer (not `Run()`) by `TestActivate_RRFDefaultThreshold_EndToEnd` (`internal/engine/engine_rrf_threshold_test.go`) plus its ACT-R control `TestActivate_ACTRDefaultThreshold_Unaffected`. *R1 (#590 follow-up): before this, `activateCore` unconditionally coerced `Threshold==0 → 0.1` BEFORE `activation.Run()` ever saw the request, making #590's mode-aware default (rrf → 0.001) dead code on every production transport (MCP, REST, gRPC, MBP, embedded) — RRF vaults returned silently-empty default recall. The pre-existing `Run()`-level regression test (`TestRRF_ReturnsResultsWithDefaultThreshold`) stayed green throughout because it calls `Run()` directly with `Threshold: 0`, a call shape production never produces — exactly why the new pin lives at the engine layer instead.* MCP additionally: when the `threshold` arg is omitted, `handleRecall` consults the vault's `ScoringFusion` and passes `0` through (not its historical flat `0.5` prefill) for rrf vaults — and likewise passes `0` whenever a recall `mode` is present, because the mode's preset replaces the 0.5 default and resolving it is the engine's decision; an explicit caller-supplied threshold at or above 0.01 against an rrf vault that returns zero results gets a loud hint (never a silent, unexplained empty), not a clobbered value. **Amendment (#704):** recall-mode presets were a SECOND threshold decider that bypassed the mode-aware default — transports stamped ACT-R-calibrated preset thresholds (semantic 0.3, recent 0.2, deep 0.1) into the request, and the engine's vault-default `recall_mode` block overrode the leave-0-for-rrf decision, so a mode-carrying recall on an rrf vault was still silently empty. Now the ENGINE is the single preset decider: transports (MCP `handleRecall`, REST `handleActivate`) validate the mode name and forward it on the wire (`ActivateRequest.Mode` — previously silently dropped for raw MBP/embedded callers, now honored; gRPC's proto carries no mode field, so that surface is unchanged, and an invalid mode from a raw wire caller is silently ignored — fail-open on presentation, matching the engine's total-ignore of the wire field before; transports rejected invalid modes then and now). The engine resolves the preset in `recall_mode_preset.go` + the weights block, where the effective scoring mode is known: under rrf fusion the preset threshold ABSTAINS (Threshold stays 0 → `Run()`'s rrf floor); preset hops apply unless the caller set `MaxHops` or explicitly `DisableHops`. Preset WEIGHTS have three deliberate cases: (1) explicit weight-carrying mode (`semantic`/`recent`) with no caller weights → the preset defines the FULL weight vector from the ZERO base — the exact struct transports used to stamp — because the preset zero-value scheme cannot express semantic's implicit "decay/hebbian/access/recency = 0", and overlaying resolved defaults lets a fresh, recently-active engram score ~0.7 under legacy scoring with zero content match, above semantic's own 0.3 threshold (silently-wrong-results class, caught in this increment's refute); (2) caller weights present (any mode source) → preset fills only wire-zero fields, caller-set fields always win; (3) vault-default mode with no caller weights → the vault's resolved weights stand and only the `DisableACTR` strategy bit applies (a background default tints, it does not respecify scoring — byte-preserving develop's effective behavior on that path, where resolved-side zero-guards made the scalar fill a no-op). Presets never change the FUSION mode (`UseRRFFusion` comes from vault config or genuinely caller-explicit weights — previously, a weight-carrying preset stamped client-side silently flipped an rrf vault to weighted-sum scoring); on non-rrf vaults an explicit weight-carrying mode does select its scoring STRATEGY (the zero-base vector carries the preset's `DisableACTR` bit), exactly as its stamped form always did. `balanced` means engine defaults: the engine skips it, and MCP keeps its historical 0.5 surface default for it. Pinned by `TestActivate_RRFVaultDefaultRecallMode_NotSilentlyEmpty` (RED without the abstention), `TestActivate_RRFExplicitWireMode_ThresholdAbstains`, `TestActivate_ACTRVaultDefaultRecallMode_PresetThresholdUnchanged` + `TestActivate_ACTRExplicitWireMode_AppliesPreset` (ACT-R paths unmoved / wire mode honored), and the transport wire pins `TestHandleRecallModeForwardedNotStamped` (MCP, RED before: `Mode==""`, `Threshold==0.1`) and `TestActivate_ModeForwardedNotStamped` (REST).
- **[COG-7]** ACT-R base-level is capped at ≈1.489 (ln(e^1.693−1)); Hebbian/transition boosts may exceed it, base-level alone may not — `activation/engine.go`. *"threshold=0.3 means the same thing in a fresh and a mature vault."*
- **[COG-8]** In RRF mode, BFS-traversed candidates must carry a non-zero rrfScore or they vanish from results — `activation/engine.go`. *Traversal reachability must not silently drop.*
- **[COG-9]** Soft-deleted/archived engrams are always filtered in phase-6 scoring and in every post-pipeline injector — `activation/engine.go`; for injections the check lives in the shared gate, `visibility_gate.go` (`visibilityGate.Nameable`). *HNSW has no delete; the filter is the only guard.* **Amendment (valid-time, planned relaxation):** under `as_of=T` the design admits soft-deleted engrams that carry an explicit validity window covering T (evolve-superseded predecessors are stamped for exactly this) — records soft-deleted by plain `muninn_forget` (no stamp) stay hidden even from `as_of`; deletion still means deletion. **The relaxation is NOT yet implemented** (deferred from valid-time increment 1): today `as_of` filters only among currently-visible engrams, so evolve predecessors are not yet resurrected by time-travel. A PR implementing it must confine the relaxed filter to the `as_of` path.
- **[COG-10]** Co-activation (recall-time Hebbian) never updates confidence — only user feedback and contradictions do — `engine.go`. *"Evidence of relevance, not evidence of truth."* **Amendment (importance):** Importance is likewise never moved by access or co-activation — only the caller sets it (write/evolve; evolve inherits the predecessor's explicit value unless overridden). `TouchAccess` still moves only AccessCount/LastAccess — never Confidence, Trust, Stability, or Importance (a Stability write-on-read was designed and then deferred: Stability feeds the weighted_sum/RRF DecayFactor score component, so it would change recall results in those modes — see the TouchAccess doc contract). The three axes stay structurally distinct: Confidence = truth, Importance = priority, AccessCount = use. The use-time `EffectiveImportance` derivation (`internal/storage/importance.go`) is never written to storage — stored 0 always means "unset".
- **[COG-11]** `actReq.ReadOnly` — the single resolved decision computed once in `activateCore` as `auth.ObserveFromContext(ctx) || req.ReadOnly` — skips the recall-event persist, the Hebbian co-activation submit, and the PAS transition write — `engine.go`. *A read must not mutate learning state, whether "read-only" comes from the credential (observe mode) or from an explicit per-call `read_only:true` on a full-mode credential — the two gates are distinct (a credential property vs. a per-call request) and both must independently suppress.* **Amendment (#598):** three of these write sites gated on `auth.ObserveFromContext(ctx)` ALONE until this amendment, silently ignoring an explicit `req.ReadOnly` — a full-mode credential's `read_only:true` recall still bonded every returned engram pairwise (the Hebbian submit) and still persisted a recall event, contradicting both this invariant's text and the `muninn_recall` tool description ("must not trigger any write side effects"). Fixed by gating all three sites on `actReq.ReadOnly` instead of re-deriving the credential check; `TestActivateCore_ObserveFromContextResolvedExactlyOnce` (`internal/engine/readonly_side_effects_test.go`) enforces there is exactly one direct call to `auth.ObserveFromContext` inside `activateCore` (the one computing `actReq.ReadOnly`), so a future write site that copies the old pattern fails immediately rather than silently reintroducing the gap. #598's separate headline claim — that co-activation learning from ordinary (non-read-only) recall retries distorts later ranking — is NOT resolved by this and needs a measurement; that half of #598 stays open. **Amendment (#846):** phase 4.75's lazy archive restore (`internal/engine/activation/engine.go`, `Run`) was the fourth write site and had NO `ReadOnly` gate at all — unlike the three sites #598 fixed, it was never gated on anything, so EVERY `read_only:true` `Activate` (not just an unguarded credential path) could mint a live 0x03/0x04/0x14 association row out of an archived (0x25) edge via `RestoreArchivedEdgesTransitive`, including that call's STO-12 stranded-row DELETE branch — the exact wound this invariant previously flagged as known and unfixed. Fixed by gating the phase 4.75 call on `req.ReadOnly` (the same resolved value `Run` already reads at its log-submission gate) — the restore still happens lazily, on the next NON-read-only `Activate` that reaches the edge, which is the correct semantics. Pinned by `TestActivate_ReadOnly_DoesNotRestoreArchivedEdge` (`internal/engine/engine_readonly_archive_restore_test.go`). **Amendment (#846, cache recency):** a ReadOnly `Activate` also warmed the L1 read cache's recency timestamp (`internal/storage/cache.go`) for every candidate `phase6Score` merely SCORES, not just ones it emits, and `EngramLastAccessNs` feeds real ACT-R recency scoring in a LATER, unrelated recall — a scoring pass is not a user access, and stamping on every scoring pass could flip an aged, genuinely-never-accessed memory to recallable purely because an earlier read-only pass had glanced at it. Fixed by `storage.ContextWithNoAccessCacheStamp`, consulted by `GetEngram`/`GetEngrams` (`internal/storage/engram.go`) via `L1Cache.GetNoStamp`/`SetNoStamp` (`internal/storage/cache.go`) — an entry is still cached (served without a Pebble round-trip on a same-call re-read) but its recency timestamp is left at zero rather than stamped `now`. `activation.phase6Score` sets it whenever `req.ReadOnly` is true, so the fix covers every ReadOnly recall, not a single caller. Pinned both directions (suppressed read does not freshen; a real read still does) by `TestGetEngram_NoAccessCacheStampContext_DoesNotFreshenRecency` / `TestGetEngrams_NoAccessCacheStampContext_DoesNotFreshenRecency` (`internal/storage/cache_access_suppress_test.go`).
- **[COG-12]** `Recall`/`Activate` never bumps engram AccessCount — `engine.go`. *Recall is not an access for decay purposes.* **Amendment (S2/#682):** AccessCount is bumped (via `store.TouchAccess`) by exactly these channels — explicit `RecordAccess`; content-dedup reinforcement on `Write`/`WriteBatch` (1/day cap); `RecordFeedback(useful=true)`; and **`Engine.Read` by id** when the read is not read-only and the vault's `ReinforceOnRead` plasticity is enabled (default true). The read channel is what finally makes decay/importance learn from use (RecordAccess was dead before). The invariant's core still holds: the multi-result `Recall`/`Activate` path never bumps — reads that reinforce are single-engram, caller-intended fetches, and observe-mode/`read_only:true` reads suppress it (COG-11, S3). A change that makes `Activate` reinforce, or that drops the read-only/observe suppression, violates this.
- **[COG-13]** Idempotency check precedes content-hash dedup on the write path — `engine.go`. *Re-submitting a known op_id with drifted content returns the original, deliberately.*
- **[COG-14]** Caller-supplied embedding ⇒ `DigestEmbed` flag set + inline HNSW insert; the retroactive embedder must never overwrite it — `engine.go`. *Don't re-embed what the caller already embedded.*
- **[COG-15]** The pruner only ever **hard-deletes**, and runs only for vaults with `MaxEngrams>0` or `RetentionDays>0` — `engine.go` PruneVault / runPruneWorker. *Soft-delete would leave relevance-index entries → infinite prune loop.* **Amendment (importance):** the MaxEngrams path skips candidates with `EffectiveImportance >= storage.HighImportanceFloor` (0.7) — see COG-20 — and when those exemptions leave `pruned < excess` it logs a **WARN** and stops (degrade loudly, no loop; a vault marking most memories high-importance cannot shrink via this path — documented residual, protected-fraction cap is the increment-2 lever). If candidate metadata cannot be loaded, the MaxEngrams pass is skipped for the cycle with a WARN rather than deleting importance-blind.
- **[COG-16]** Association decay requires `HebbianEnabled && AssocDecayFactor>0` — `engine.go`. *Decay of learned edges is gated on learning being on.* **Amendment (#762):** `AssocDecayFactor` is now **only that switch** — it no longer carries a rate. The rate is `AssocHalfLifeDays` (COG-27). The gate expression is unchanged, so `scratchpad`'s `0` still disables decay with no special case. A vault whose switch is on but whose half-life resolves to `0` is **skipped with a one-time WARN**, never given a substituted default (principle #1); a vault with an *explicit* legacy `assoc_decay_factor` in (0,1) and no half-life has it reinterpreted **per-day** (`halfLifeDays = ln0.5/ln f`; 0.95 → 13.51 d) with a one-time-per-vault WARN naming the derived value and the field to set; an explicit factor **≥ 1** carries no rate at all ("on, but weights never move") and resolves half-life **0** — the skip-with-WARN path, with its own truthful message, never the preset's rate and never a `derived_half_life_days` that was not derived. Pinned by `TestDecayAllVaults_LegacyFactorWarnsOncePerVault` / `..._LegacyFactorAtOne_SkipsWithoutSubstitutedRate` / `..._NoHalfLifeSkipsAndWarns` / `..._ExplicitHalfLifeDoesNotWarn` (`internal/engine/engine_assoc_decay_config_test.go`).
- **[COG-17]** Hebbian weight updates are log-space multiplicative, capped at 1.0, seeded at 0.01 — `internal/cognitive/hebbian.go`. *Prevents overflow and runaway weights.*
- **[COG-18]** Entity boost is rarity-weighted, capped, and contract-gated: each shared entity contributes `0.15 × idf` (idf vault-local-n / store-global-df, so ubiquitous entities contribute ~0), credited once per target regardless of seed count; total boost per engram is capped at 0.30 (low end of the BFS weight range — saturated entity evidence may tie the weakest genuine edge, never a mid-strength one); seeds are the TopN=5 results that themselves cleared the threshold (a tag-bypass admission must not seed); injections must clear the caller's threshold and the rest of phase 6's result contract via the shared visibility gate (COG-22) — meta filters, `ExcludeUntrusted`, lease visibility (#548), the structured (MQL WHERE) filter, and the COG-19 valid-time gate; results are re-truncated to MaxResults after boosting — `engine_entity_boost.go`, `visibility_gate.go`, `engine.go` (tightened in #581, reworked in #570). *Ubiquitous entities must not flood recall, and the boost path must honor the same result contract as phase 6 (#569, #654).* **Amendment (R1, #590/#570 follow-up):** the boost pass is explicitly skipped under rrf fusion (`req.Weights.UseRRFFusion`) — its factor/cap/noise-floor are calibrated to ACT-R's [0,1] scale, and rrf finals are rank-based, typically <0.05; before R1 this was an accidental no-op (the pre-fix 0.1 threshold coerce meant no rrf result ever cleared the seed rule — see COG-6), now made deliberate so that reaching rrf's genuine ~0.001 default threshold does not let a +0.30-scale boost dominate and outrank genuine content matches. Pinned by `TestApplyEntityBoost_SkippedUnderRRFFusion` (`internal/engine/engine_rrf_threshold_test.go`; the skip guard itself lives at the top of `applyEntityBoost` in `engine_entity_boost.go`, cross-referencing that file's CALIBRATION CAVEAT comment), with an ACT-R positive control proving the same evidence injects when the skip is absent. Recalibrating the boost for rrf's scale remains a scoped follow-up (#570).
- **[COG-19]** Invalidation is always a `ValidUntil` stamp, never a delete; default recall never returns an engram whose `ValidUntil <= now` — the single shared predicate is `storage.Engram.ValidAt`/`IsExpired` + `activation.PassesValidity`, enforced in phase-6 (`activation/engine.go`, beside `passesMetaFilter`) AND at the final post-entity-boost gate (`engine.go` `activateCore`), pinned by `TestCOG19_Pin` (`internal/engine/valid_time_test.go`). Stamp surfaces: `Evolve` (batch `SupersedeEngram` — soft-delete + stamp, atomically), explicit `RelSupersedes` links (skip-if-closed), `muninn_forget.not_true_since` (overwrite), `muninn_restore` (clears). *Validity is a HARD filter; cognition ranks only survivors. A new recall-adjacent surface (entity_state, where_left_off, brief, find_by_entity) that returns engrams without this predicate can lead with an expired fact — the "May figure" bug — so gate coverage is a per-surface adoption obligation (increment 2). The default gate excludes only EXPIRED facts, never future-`ValidFrom` ones.*
  *#720 amendment (the stamp is also the TRASH/HISTORY discriminator).* Because invalidation is a stamp rather than a delete, `State == StateSoftDeleted` alone says nothing about WHY a record is gone: `Evolve`/`RelSupersedes` soft-delete AND close `ValidUntil` in one batch and deliberately leave the record's FTS postings in place (they are the keyword path a query phrased in the OLD wording still reaches, resolved forward to the chain head by COG-28), while `muninn_forget` without `not_true_since` leaves the stamp OPEN and de-indexes at delete time. A maintenance path deciding whether to DROP or REBUILD an engram's index entries must therefore key on the stamp, **never on `State`** — `activation.CarriesSupersessionSignature` (`internal/engine/activation/history_gate.go`) is the single predicate, factored out of `PassesLifecycle` rather than reused wholesale because that one also takes the `asOf`/`includeInvalid` view parameters, which a write path has no business consulting. `Engine.UpdateTags` is the current caller; a retag that keyed on `State` destroyed a superseded fact's postings permanently — recoverable only by `reindex-fts`, and invisible until an `as_of` query that used to work stopped working. `StateArchived` rebuilds for the same reason: nothing de-indexes on archival, so dropping there destroys live postings. Pinned by `TestUpdateTags_EvolvePredecessorKeepsPostings` and its negative control `TestUpdateTags_TrashIsStillDeIndexed` (`internal/engine/engine_update_tags_review_test.go`), RED-checked against the `State`-only gate. Composes with COG-27's `NameableAsLineage`, which reads the same closed-stamp signal on the READ side.*

- **[COG-20]** An engram with `EffectiveImportance >= 0.7` (`storage.HighImportanceFloor`) is **never hard-deleted by the MaxEngrams (retrieval-strength) prune path**; `RetentionDays` remains an authoritative age policy, exempt from this protection — `engine.go` PruneVault, pinned by `TestPruneVault_COG20_HighImportanceExempt` (`internal/engine/engine_pruning_test.go`, RED-checked: fails with the exemption disabled). The exemption performs **no validity check**: an expired-but-important fact survives capacity pruning so `as_of` time-travel still has a destination (composes with COG-19 — invalidation is a stamp, decay/prune protection is orthogonal). Effective importance = explicit caller value (stored > 0; explicit 0 is quantized to 0.01 at write) else the memory-type table in `internal/storage/importance.go` (+0.1 when trust=verified) — the derived value is use-time only, never stored. **Scope note (increment 1):** importance drives ONLY this prune protection plus read-surface exposure — it does not touch decay rates, reinforcement, or recall scoring. Importance-modulated decay is deferred until designed against all three scoring modes (ACT-R, weighted_sum, RRF): Stability/Relevance feed the DecayFactor score component in weighted_sum/RRF, and a d_eff modulation in ACT-R lowers B(M) for fresh memories (ageDays < n+1 flips the sign of the −d·ln term) — both would silently change which results recall returns.
- **[COG-21]** A prospective notice fires ONLY when its cue entity is in the focal set of the current call — focal = entities carried by the TOP-scored returned result, or corroborated by ≥2 returned results (recall), or the caller's inline entities (remember). An armed intention's own engram (a TypeGoal engram that carries its own cue as a first-class entity) does NOT self-satisfy its own focality: its contribution to the top-result path or the ≥2-carrier corroboration count is excluded for cues it is itself armed on (`storage.IsIntentionArmedOnCue`, a 0x2D point lookup) — an intention fires only via OTHER results genuinely carrying its cue. This does not exclude TypeGoal engrams generally; a goal engram remains a legitimate corroborator of a *different* intention's cue, or of its own cue via genuinely separate carriers. Unrelated context ⇒ zero notices, always; nothing polls the armed-intention index (0x2D), there is no scheduler, and `valid_until` SILENCES an expired intention rather than firing it — `internal/engine/prospective.go`, pinned by `TestProspective_FocalGateInvariant`, `TestNoticesForRecall_SelfFocality_IntentionOwnEngramAlone`, `TestNoticesForRecall_SelfFocality_SelfCorroboration`, `TestNoticesForRecall_GenuineCorroborationStillFires` plus the acceptance harness (`prospective_harness_test.go`: precision ≥0.90, recall ≥0.80, 35 unrelated calls → exactly zero, and a Gate-4 RED arm proving the harness fails with the mechanism off). Composes with COG-11: `read_only`/observe delivery suppresses the fired-marker write (the one-shot stays armed; session dedup covers the session), pinned by `TestNoticesForRecall_ReadOnlySuppressesMarker`. The whole delivery path is opt-in (`MUNINN_PROSPECTIVE=1`); `muninn_intend` refuses cues mentioned by ≥10% of a ≥20-engram vault (the IDF arming floor — never arm a nag). *The #609 lesson made structural: no task-blind push exists to misfire. #693: self-retrieval must not fire a notice on nothing but itself.*

- **[COG-22]** Post-pipeline injection is gated and supersession substitutes atomically under the caller's view. Every result appended after phase 6 (entity boost pass 2b, supersession heads) passes the shared `visibilityGate` (`internal/engine/visibility_gate.go`), which delegates to phase 6's own predicates — never a re-implementation. The gate is two-layered on purpose: `Nameable` (state, meta filters, trust, structured filter, lease) governs whether an ID may surface anywhere the SUPERSESSION RANKING PHASE emits — result rows and its always-on `SupersededBy`/`CurrentVersion` annotations alike — for these predicates the ID itself is the secret (#548); `ValidForView` (COG-19) governs only what may be presented as CURRENT, because supersession stamps every superseded engram and expired chain intermediates are legitimate, nameable lineage. Supersession resolves its head by walking chains through hidden nodes without ever naming them (deepest nameable node valid for the view wins; under `as_of`, nodes postdating the instant may not even be named), and a chain with no such node abstains WHOLE — no injection, no demotion, no `SupersededBy`/`CurrentVersion` annotation. Half-applied substitution is the forbidden state: demote-without-inject silently truncates the topic, and an annotation naming a hidden node is the #548 leak in metadata form. Admitted injections count in `TotalFound` when appended (known deviation, both injectors: a pipeline-found row truncated past MaxResults inside Run() and re-injected is counted twice). **Formerly-ungated surface, closed by #700:** the opt-in `annotate:true` fallback (`GetAnnotations` → `augmentAnnotations`) used to fill `SupersededBy`/`ConflictsWith` from a raw reverse-edge lookup, bypassing every predicate above. `GetAnnotations` now takes the caller's own `*mbp.ActivateRequest` and routes every candidate target through the identical `visibilityGate.Nameable` (`internal/engine/annotation.go` `annotationVisibilityGate`) before naming it; a refused target is silently omitted from the annotation, matching how the ranking phase's own gate failures behave. Pinned by `TestEngine_GetAnnotations_LeaseHiddenConflictTargetNotNamed` (RED-checked: fails with the gate bypassed). Pinned by the guard tests in `engine_supersession_test.go` — the per-predicate guards each RED-checked against their bypassed check, `ViewFuture` pinned by its own backdated-successor test — and `TestEntityBoost_InjectionRespectsStructuredFilter`. *An injector that sees less than the pipeline re-opens phase-6 holes one field at a time (#654, the #570 rounds, the structured filter — three review cycles, same class); the gate exists so the next visibility field lands in exactly one place.* **Amendment (COG-28, #763):** a declared-supersession predecessor may be NAMED as substitution provenance under `visibilityGate.NameableAsLineage` — `Nameable` with the lifecycle predicate evaluated as `PassesLifecycle(eng, asOf, includeInvalid=true)`, which admits a soft-deleted record ONLY when it carries a closed `ValidUntil` (still refusing `StateArchived` and open-stamp forget trash). Same reasoning that already admits expired chain intermediates as lineage, plus two facts specific to this case: the predecessor's ID is already publicly derivable from the head (`EvolveAt` writes provenance `Details.PredecessorID` on the successor and `muninn_read` surfaces it), and refusing to name it makes the row SILENTLY substituted, which is the worse failure. **Every other existence predicate is unchanged** — meta filters, trust, structured filter and lease are untouched, and a predecessor refused by any of them is not even a shadow. It is a separate method, never a loosening of `Nameable`, so the bad state stays unrepresentable for every other injector (principle #3). Pinned by `TestVersionHead_PredecessorWordingReturnsHead` (RED-checked: swapping `NameableAsLineage` for `Nameable` makes it fail) and `TestVersionHead_PredecessorUnderForeignLeaseIsNotEvenAShadow`.

- **[COG-23]** Contradiction severities come only from the `ContradictionSeverity` relation-matrix (1.0 direct negation e.g. Supports↔Contradicts, 0.9 incompatible-relation-types e.g. PrecededBy↔FollowedBy) or an explicit `RelContradicts` link (`Engine.Link`, `engine.go` ~2740, which submits a matrix-eligible pseudo-pair through the same `ContradictWorker.processBatch` path) — **never synthesized from same-relation-type/different-target association shape**. An ordinary write linking one engram to multiple targets under the same RelType (e.g. two `references` associations to two different engrams) produces **zero contradiction flags and zero confidence penalty** — `internal/cognitive/contradict.go` `processBatch`, pinned by `TestWrite_TwoTargetsSameRelType_NoFabricatedContradiction` (`internal/cognitive/contradict_fabrication_test.go`, RED-checked: fails if the deleted same-RelType/different-TargetHash rule is restored) plus positive controls (`TestWrite_GenuineMatrixContradiction_StillFlags`, `TestWrite_GenuinePrecededByFollowedByContradiction_StillFlags`, `TestWrite_ExplicitContradictsLink_StillFlags`) proving real detection is untouched. *R2 (#611-class, severity raised on verification): before this, `contradict.go` fabricated a 0.8-severity contradiction for ANY two same-RelType associations from one engram to two different targets — no semantic signal consulted — which persisted a 0x0A `FlagContradiction` marker, surfaced via `muninn_contradictions`, and dragged BOTH engrams' stored confidence down via `EvidenceContradiction` (weight 0.1). The write-path trigger notification for this class of event was also mislabeled `"semantic"` (`engine.go` ~1357, ~1901) despite being a pure relation-matrix heuristic; renamed to `"relation_matrix"` (the explicit-link path's `"explicit_link"` label is untouched).* **Residual:** previously-fabricated 0x0A contradiction markers in existing vaults are not migrated or auto-cleared by R2 — they remain resolvable via the existing `ResolveContradiction` path (a one-time auto-cleanup is unsafe: the 0x0A marker persists only the pair, no severity/type/provenance, so fabricated markers are mechanically indistinguishable from genuine explicit-link ones). Likewise, the `EvidenceContradiction` confidence penalty already applied to existing engrams is not reversed — `BayesianUpdate` is not safely invertible without the pre-penalty value, which was never persisted.

  *#764 amendment (D1).* The Link→`ContradictWorker` path above is wired correctly and always was, but until #764 nothing downstream of it ever ran under load: `Worker[T].Run` reset its flush ticker on EVERY received item, turning "flush at least every `maxWait`" into "flush after `maxWait` of SILENCE". `Engine.Write` submits a `ContradictItem` on every remember, so an agent writing anything more often than the 30s interval held the flush off until the batch reached `batchSize` (50) — which an ordinary session never reaches. A declared contradiction therefore sat undetected indefinitely, and `ConfidenceWorker` (the same generic) starved identically, so #747's "the penalty now arrives up to one worker interval later" was in practice "the penalty never arrives". Fixed by resetting the ticker only on the transition INTO `WorkerStateActive` (`internal/cognitive/worker.go`), pinned by `TestWorker_ActiveSessionDoesNotStarveFlush` + `TestWorker_IdleFlushStillFires` (`internal/cognitive/worker_starvation_test.go`) and `TestContradictionDetection_SurvivesAnActiveSession` (`internal/engine/engine_contradiction_detect_test.go`), all RED-checked at `bb10f30`. **Do NOT "fix" this by writing the 0x0A marker synchronously in `Engine.Link`.** `FlagContradiction`'s `newlyFlagged` return IS the confidence penalty's idempotency token: a synchronous write makes the worker's later call return `newlyFlagged=false`, and the penalty then never fires at all — reverting #746's idempotency guard into a silent no-op. The reported status was also renamed `pending_detection` → `declared` (with a sibling `confidence_penalty: pending|applied`), because after #764 the only thing ever outstanding about a declared contradiction is that penalty — the contradiction itself is durable at `Link` return and honored by recall on the next query (COG-29).

- **[COG-24]** `full_text_relevance` is an **absolute, query-calibrated coverage score in [0,1]**: `Score(d) = (Σ_t idf_t'·cov_t(d)) / (Σ_t idf_t')`, where `t` ranges over the query's deduplicated, tokenized (stemmed-or-raw) content terms; `cov_t(d)` is term `t`'s field-weighted BM25 coverage in doc `d`, capped at 1 (`min(1, Σ_fields tfNorm·fieldWeight/(k1+1))`); and `idf_t'` is the term's real IDF, except a term with **no corpus term-stats entry at all** (`getIDF` returns 0 — the corpus has never seen it), which is charged `idfMax = ln((N+0.5)/0.5 + 1)` — the IDF the term would have at the maximum possible rarity (df=0) for this corpus size — and contributes 0 to the numerator. The denominator depends **only on the query and the corpus's IDF statistics, never on the result set** (no per-query-max normalization) — `internal/index/fts/fts.go` `Search`/`searchTokenCoverage`. Consequently the recall threshold is a genuine abstention gate: a query whose candidates all score below threshold returns empty, and matching only a common token cannot clear a default threshold by itself. The legacy stemmed/raw dual-path union (for vaults not yet reindexed, `FTSVersionKey`) is treated as **one term group per query word**, not two: a genuinely absent word is charged `idfMax` exactly once regardless of how many index-path forms were tried, but each form that DOES have real postings still contributes its own coverage (both forms can carry real, independent postings on a mixed legacy/reindexed vault — `TestFTS_DualPathSearch`). Pinned by `internal/index/fts/calibration_test.go` (single-common-token low relevance, full-coverage high relevance, absent-term-uses-idfMax, and the anti-per-query-max independence pin) and the end-to-end RED/GREEN pair in `internal/engine/activation/fts_abstention_test.go`. *#711: before this, `full_text_relevance` was `math.Tanh(raw unbounded BM25)` (three call sites in `internal/engine/activation/engine.go`: `computeComponents`, `computeACTR`, and the RRF-path reporting site) — raw BM25 runs 2–40 in practice and tanh saturates by x≈3, so a single common word (bm25≈5) reported ≈0.9999, indistinguishable from a genuine multi-term match, and a query term absent from the corpus was silently skipped (`idf<=0 → continue`) rather than penalizing. That let a nonsense query with one coincidental common-word hit report a confident match with nothing to filter it — no threshold can gate a dishonest ≈1.0.* **Deferred, named honestly (not closed by this increment):** (1) the semantic-similarity floor — weak-but-nonzero cosine similarity between unrelated texts (embedding anisotropy) still floats some irrelevant results when semantic weight is nonzero; (2) recency/decay components floating a near-zero-relevance engram in the legacy weighted-sum path (ACT-R's contentMatch gate already prevents this); (3) the #331 per-query max-rescale safety net in ACT-R (`engine.go` ~1690, fires only when a candidate's raw exceeds 1.0 — verified NOT to re-trigger on the nonsense-query RED case post-fix, since low contentMatch no longer approaches the cap); (4) RRF-mode abstention (rank-based scores cannot abstain by construction, unaffected by this change). **Residual finding (measured, not yet resolved):** in FTS-only degraded mode (no HNSW/embedder — semantic weight redistributed onto `FullTextRelevance`, ceiling ≈0.385–0.4) against memories stored with **no `concept` field** (content-only), a single-occurrence term match structurally caps `cov_t` around `fieldWeightContent/(k1+1) ≈ 0.45` (concept-field matches reach the 1.0 cap easily via `fieldWeightConcept=3.0`; content-only matches usually do not) — this dropped `TestProspectiveAcceptance_Gates1to3`'s 60-call harness (COG-21's cited acceptance proof) from precision/recall 1.0/1.0 to 0/0 against its `Threshold: 0.35` cutoff, which was implicitly calibrated to the old tanh-saturated near-1.0 ceiling. Left unresolved deliberately for the maintainer's real-vault measurement pass rather than silently retuned.
  *Corpus-statistics maintenance is part of this invariant, not housekeeping: `N` and `df_t` are the only inputs to `idf_t'` that do not come from the query, so a path that rewrites an ALREADY-indexed engram must leave them alone except where the document's term membership genuinely changed. An in-place edit goes through `fts.ReindexEngram` (one `idx.mu`, one batch, no `UpdateStats`, `df_t` moved only for terms that entered or left — membership computed per TERM over the whole document, since a tag token can coincide with a content token), never `DeleteEngram` + `IndexEngram`: `IndexEngram` bumps `TotalEngrams`/`AvgDocLen` AND increments `df_t` for every term of the document while `DeleteEngram` decrements neither, so the pair moves `(N, df) → (N+1, df+1)` per call. Since `getIDF` ≡ `ln((N+1)/(df+0.5))`, the `N` drift is second-order but the `df_t` drift is first-order: measured on a 60-engram vault, one retag cost the target 8.1% of its score for a query on its own UNCHANGED content, ten took it below a 0.3 threshold, and 100 cost 82.6% against a 10.0% drop for an unretagged control in the same vault. A memory that becomes unfindable BECAUSE its owner curates it is the same silently-wrong class as a false positive and nothing surfaces it. **Exactly one exception, and it is named by mechanism rather than by intent:** a FULL-VAULT rebuild that first range-tombstones the 0x08 global-stats and 0x09 per-term-stats prefixes via `store.ClearFTSKeys` may then call `IndexEngram` on already-indexed engrams. `Engine.ReindexFTSVault` does exactly that (`internal/engine/engine_reindex_fts.go:67` clears, `:80` re-indexes) and is correct because with those keys gone `IndexEngram` rebuilds `N`/`AvgDocLen`/`df_t` from zero instead of double-counting onto a live value. The exception is `ClearFTSKeys` — NOT "unless the stats are handled": a path that has not range-tombstoned 0x08/0x09 violates this clause regardless of what it believes about its own bookkeeping. One further honesty note: the neutrality this clause requires is with respect to retag COUNT, not tag CONTENT. `ReindexEngram` calls no `UpdateStats`, so `AvgDocLen` does not track in-place length changes and a tag set that grows in place is length-penalized against a stale corpus average — conservative direction (matches under-credited), cleared by `reindex-fts`, measured numbers in `ReindexEngram`'s doc comment. Pinned by `internal/index/fts/reindex_test.go` (df/stats arithmetic term by term, plus the `adjustDF` zero clamp) and `internal/engine/engine_update_tags_fts_test.go` (the end-to-end score, through `Engine.UpdateTags`) — #720.*

- **[COG-25]** Heuristic currency is **advisory-only**. The recall-time version-cluster detector (`internal/engine/engine_currency.go` `applyCurrencyAnnotation`, #712/#716) never removes, demotes, score-changes, writes, stamps, links, or confidence-penalizes any result; it only **annotates** (`possibly_superseded_by` / `version_cluster` / `newest_of_cluster` / `cluster_size`) and may reorder **exact score ties within a single detected cluster**, newest-`EffectiveValidFrom`-first. The reorder is a localized post-pass over each equal-score run that permutes only the slots already occupied by one cluster's tied members (`currencyReorderTieRun`) — it moves no non-member and changes no truncation membership; the base sort is a pure total order (`Score` desc, `ULID` asc) so ordering stays transitive and run-to-run deterministic (#699). An explicit `RelSupersedes` (evolve / link) **always wins and suppresses the heuristic entirely for every engram it touches, on BOTH sides of the chain** (`currencyInDeclaredChain`, R4/#757-follow-up): an engram carrying any `RelSupersedes` edge in either direction is excluded from heuristic clustering altogether, so it can neither receive `possibly_superseded_by` nor be crowned. The pre-R4 rule suppressed only the asserted *pair* (plus a crown re-check at emission) and therefore left the declared **successor** — the head of an evolve chain, which carries no incoming stamp — indistinguishable from an un-versioned engram, letting an arbitrary newer unverified memory be crowned over it. That is measured-false-advisory case 3. A future-`ValidFrom` member is never crowned `newest_of_cluster`. Clustering requires, on **every** path: temporal separation, a cosine sanity floor, **competing version-marker sets** (`currencyVersionMarkerGate` — a UNIVERSAL gate since R4; it previously lived inside the tag anchor, which the entity and `RelRefines` anchors bypass by returning early, so two memories sharing one rare entity and no version vocabulary at all could be clustered — measured-false-advisory cases 1/2/4), **no exclusive facet tag at df≥`currencyFacetDF`**, and a shared **topic** anchor (≥2 shared non-ubiquitous tags, or a shared rare entity, or a `RelRefines` edge — anchors substitute for the shared-tag test only, never for version vocabulary); "ubiquitous" is **self-derived per vault** as `max(currencyUbiquityRatio·N, currencyUbiquityAbsMin)` (the absolute floor is #11: a tag on <10 memories is never ambient regardless of vault size), and a tag-df lookup failure fails **toward silence** (returns a large df → the tag is dropped as ambient AND vetoes as a facet), never toward a fabricated cluster. Annotations/`newest_of_cluster` are computed over the **co-retrieved result set only** (same returned-set boundary as the authoritative `SupersededBy`). Surface scope: MCP + MBP annotations only (matching the pre-existing `superseded_by` scope; REST inherits via type alias; proto/gRPC/SDK carry no supersession annotations). Pinned by (all RED-checked) `TestCurrencyAnnotation_ExplicitSupersedesCrown_NoTransitiveLeak`, `..._DigitPrefixedFacet_NotExemptFromFacetVeto`, `..._TieBreak_TransitiveAcrossClusterBoundary`, `..._FacetConflict_IsLoadBearing`, `..._TemporalFloor_/CosineFloor_/TagShareMin_…NotClustered`, `TestCrossVault_SmallVault_SelfDerivedUbiquity_ChainClusters`, `TestCurrencyUbiquityCutpoint`, and the R4 precision suite `TestCurrencyPrecision_EntityAnchorSideDoor_UnrelatedTopics` / `..._RelRefinesSideDoor_NoVersionVocabulary` / `..._DeclaredSuccessor_NotAdvisedSuperseded` / `..._AdjacentTopics_NoBroadHints` / `..._GateAttribution` (`internal/engine/engine_currency*_test.go`), plus the cross-vault safe-silence corpus. *No mechanical signal can prove "B replaces A" vs "B supplements A" — that is a judgment about the world (the five-analysis impossibility result); this phase supplies a high-precision hint and the querying agent supplies the judgment.*
- **[COG-26]** `semantic_similarity` is an **absolute, model-calibrated relevance in [0,1]**: `semCal = max(0, (cos − b)/(1 − b))` where `cos` is the raw cosine returned by the vector index and `b` is the embedding model's measured anisotropy noise baseline, resolved from a per-embedder registry (`internal/plugin/embed/baseline.go`, `NoiseBaseline`/`Rescale`) keyed by the vault's embed model. Model resolution order (`internal/engine.(*Engine).resolveSemanticBaseline`): (1) the per-vault plasticity override `SemanticFloor`/`SemanticFloorOverride` (explicit `0` disables the floor; an out-of-range value ≥1 is rejected with a WARN and falls through, never silently zeroing every match); (2) the vault's recorded embed model (`store.GetEmbedModel`) if set, else the engine's process-wide `EmbedModelName` (resolved once at startup from the active provider, `cmd/muninn/server.go` — for the bundled zero-config default this is `"bge-small-en-v1.5"`, registered at `b=0.520`); (3) the registry lookup itself. **A model with no registry entry — including empty/unknown, e.g. a vault whose embed-model marker was never populated and no process-wide model was wired — gets the identity transform (`b=0`) plus a one-time-per-model WARN, never a guessed floor** (#582/#585/#589: explicit config is honored or fails loudly, never silently substituted). Applied at the three sites where `w.SemanticSimilarity*vectorScore` feeds `contentMatch`/`raw` (`internal/engine/activation/engine.go`: `computeComponents`, `computeACTR`, and the RRF-path reporting site — the same trio COG-24 touched for tanh; `computeGatedActivation`/CGDN inherits the calibrated value for free by reusing `computeComponents`'s output rather than recomputing it, per the reuse-proven-mechanisms principle). `FullTextRelevance` is untouched (already calibrated by COG-24). Reported `score_components.semantic_similarity` is the CALIBRATED value (`semCal`, not raw cosine) — mirrors COG-24's `full_text_relevance` semantics — and `score_components.semantic_similarity_raw` carries the uncalibrated cosine alongside it at every site (the honesty backstop: an operator debugging a wrongly-abstained match can see the raw cosine, e.g. 0.59, next to the calibrated value that made it abstain, e.g. 0.07, without a second tool call). Consequently the ACT-R content gate (`engine.go` line ~1931: "zero semantic relevance = zero score regardless of recency") now gates semantic nonsense the same way COG-24 gates lexical nonsense, and corroborated weak matches survive by construction — FTS coverage still adds to `contentMatch`, Hebbian/transition boosts still multiply the prior. Pinned by: the bge-small registry-constant value; `internal/engine/activation/semantic_abstention_test.go` (RED-sanity with `SemanticBaseline=0` reproducing the pre-fix leak on a real bge-small embedder + real cosine, GREEN with `SemanticBaseline=0.520`, positive controls proving a genuine high-cosine match and a genuine paraphrase both still clear the floor as hard assertions — not skips, see below — a dedicated 0.60–0.65-band genuine-match pin, and a small-vault (~18 engram) zero-lexical-overlap probe); the rescale is monotone in `cos` (`Rescale`/`rescaleSemantic`), so RRF ranks are provably unchanged and genuine orderings cannot invert.
  *#711/COG-26: this closes deferral (1) from COG-24 ("the semantic-similarity floor — weak-but-nonzero cosine similarity between unrelated texts (embedding anisotropy) still floats some irrelevant results"): bge-small-en-v1.5 out-of-domain query↔passage cosine runs μ=0.450/σ=0.054 (432 pairs), clearing the 0.1 ACT-R threshold on noise alone (0.6×0.5=0.30).*
  **Honest history — b was revised down after a real regression, not tuned in the abstract.** The value landing with this increment was originally `b=0.558` (μ+2σ). An adversarial refute (2026-07-29) proved that value too aggressive using a real bge-small embedder, not a synthetic one: at `b=0.558` the sem-only survival bar — the raw cosine below which `contentMatch` can never clear the 0.1 gate with zero FTS/tag/Hebbian corroboration — sits at `0.558 + 0.1667×(1−0.558) ≈ 0.632`. A genuine paraphrase measured at cosine 0.59 scored `contentMatch = 0.043 < 0.1` and **recall returned EMPTY** where the match should have ranked #2. Modeling the true-pair cosine distribution as μ=0.693/σ=0.088, roughly 24% of genuine matches fell below that 0.632 bar. The regression was previously mischaracterized in this entry as *only* a large-vault HNSW-pool-loss ranking issue (see the named residual below) — that is real, but it is not the whole story: **the floor itself, independent of any pool-loss, silently removes in-pool, otherwise-uncorroborated genuine matches below its own survival bar.** On a large, mature vault this is often masked (the same weak match is frequently outside the `K=⌊√N⌋` HNSW candidate pool anyway, so the floor isn't the proximate cause of its absence); on a small or fresh vault — a new user's first session, a narrow single-topic vault — there is no such masking and no FTS/tag/Hebbian corroboration to fall back on, so the floor is the sole gate and a too-high `b` is a genuine, measurable recall regression, not a theoretical one.
  `b` was lowered to **μ+1.3σ = 0.520**, which drops the survival bar to `0.520 + 0.1667×(1−0.520) ≈ 0.600`. This rescues the refute's 0.59-cosine case and the broader 0.60–0.65 band while still abstaining the measured noise ceiling (the highest observed out-of-domain cosine in the 432-pair measurement, ≈0.596, "quarterly ballet recital"-shaped queries against an unrelated corpus) — but the margin there is **thin**: at `b=0.520`, cosine 0.596 rescales to `semCal=(0.596−0.520)/0.480≈0.158`, `contentMatch=0.6×0.158≈0.095`, which clears the abstention gate by only ≈0.005. This is not "zero net regression" and this entry does not claim it is. **The band `cosine ≤ noise-top (~0.596)` is IRREDUCIBLE at the model-anisotropy level**: no flat, per-model scalar floor can separate a genuine match from noise inside a range where the two distributions overlap — raising `b` re-introduces the false-abstention this refute found; lowering it further shrinks the false-abstention band but shrinks the noise-rejection margin further, and eventually lets noise back in. This is an accepted precision/recall trade, made as explicit, bounded, and reviewable as this paragraph — not a hidden cost. **The recourse for a vault that needs a different point on that curve is the per-vault plasticity `SemanticFloor` override**, not a global re-tune.
  **Named residual (ranking, not abstention, NOT fixed by this increment):** genuine weak paraphrases to memories with a large in-domain candidate pool can *additionally* lose the `K=⌊√N⌋` HNSW pool to in-domain-adjacent noise and be prior-suppressed even when pooled — a monotone per-candidate rescale cannot change cosine order or pool membership, so this is a corroboration/retrieval-coverage problem (entity/association expansion, larger pool + rerank), not something a baseline floor can address, and it compounds with (does not replace) the floor's own irreducible sub-band above. **Also deferred, named honestly:** (1) a per-vault empirical calibration command (`muninn vault calibrate`) for embed models outside the registry, instead of falling back to identity+WARN forever; (2) the weighted-sum path's additive recency/decay reachable without content match (COG-24 deferral (2)) still floats independent of this floor; (3) RRF-mode abstention remains structurally impossible (COG-24 deferral (4)), unaffected by this change.

- **[COG-27]** **Association decay is cadence-independent and never raises a weight.** For every 0x03 edge, `DecayAssocWeights` computes a peak-anchored elapsed-time ceiling and clamps under it: `Δt = max(0, now − anchor)`, `ceiling = max(peakWeight·0.05, peakWeight·2^(−Δt/H))`, `w_new = min(w_old, ceiling)`, where `H` is the vault's resolved `AssocHalfLifeDays` and `anchor` is `lastActivated`, falling back to `createdAt`, falling back to *adoption* (stamp `lastActivated = now`, leave the weight alone) — `internal/storage/association.go`. Both inputs already live in the 26-byte association value: **no value-format change, no new Pebble prefix, no migration.** Consequences, each pinned:
  - Two evaluations at `t₁ < t₂` with no intervening activation produce the weight of a single evaluation at `t₂` to within `assocDecayWriteEpsilon` — the write-skip can leave the stored weight up to one epsilon above the true ceiling, never below it (`TestDecayAssoc_CadenceIndependent`). The prune worker's ~60 s period is therefore a responsiveness choice, not a term in the decay rate. *Before #762 it was the unit: `0.95` per pass at 1329 passes/day meant a ~15-minute half-life, and the store-wide maximum association weight was the 0.05 floor.*
  - Decay never increases a weight (`TestDecayAssoc_NeverRaisesWeight`, `..._FutureLastActivated_NeverBoosts`, `..._SubFloorStaleEdge_NotRaisedToFloor`). Two guards make this structural rather than policy-checked (principle #3): the drop guard (`drop = w_old − ceiling`, write nothing unless `drop >= assocDecayWriteEpsilon`) covers every path where `w_new` is the ceiling, and the post-floor guard (`newW >= oldW` after the floor/archive block ⇒ no write, excluding archive promotion and the no-peak delete path) covers the floor branch, which ASSIGNS `dynamicFloor` and could otherwise raise a sub-floor weight — reachable, since the dream engine writes inferred weights through `UpdateAssocWeight` under a monotone peak. The same guard is what stops a floored edge from being rewritten (5 keys + a replicated batch) every pass forever (`TestDecayAssoc_FlooredEdge_NoRewriteChurn`). Together they leave no path that can write an increase. It also means **#762's existing damage is not undone** — floored edges stay floored and re-learn through Hebbian growth rather than being resurrected by fiat.
  - `lastActivated == 0` means *unknown*, never *the Unix epoch* (`TestDecayAssoc_LegacyZeroLastActivated_Adopted`). Read as an epoch stamp the formula floors every legacy edge on its first pass.
  - The `assocDecayWriteEpsilon` (0.001) write-skip forgives no decay: it is a **bounded lag, not accumulated error**, because every pass recomputes from the anchor instead of compounding off the stored weight. Measured at ≤ epsilon after both 1,329 and 39,876 skipped passes (`TestDecayAssoc_EpsilonSkipLagIsBounded`).
  - Restart- and downtime-immune, and convergent under replication: the ceiling is a pure function of `(peakWeight, lastActivated, now)`, the first two replicate, and `min` is commutative and associative. This **does not close #760** — the worker is still role-blind and writes still race — but independently-scheduled nodes now converge to `min` over the union of their evaluation times instead of diverging by pass count. **Correction:** that convergence argument silently assumed `lastActivated` is *locally produced*, which stopped being true when `AssocWeightUpdate.LastActivatedAt` (#779) reached disk — the replication coordinator builds its `CoActivationEvent` from `time.Unix(0, effect.Timestamp)`, the remote peer's clock verbatim, and the Hebbian worker's per-pair max is taken only *within* a batch. **`lastActivated` is therefore monotonically non-decreasing at both writers** (`assocMonotonicAnchor`, `internal/storage/association.go` — the same shape `peakWeight` already had beside it), which is what makes the claim true rather than assumed: `max` is commutative and associative, so nodes converge on the anchor regardless of delivery order, and a lagging peer or a post-partition cog-forward backlog cannot move another node's anchor backwards into an irreversible ceiling collapse. Pinned by `TestUpdateAssocWeightBatch_NeverMovesTheDecayAnchorBackwards` / `TestUpdateAssocWeight_NeverMovesTheDecayAnchorBackwards` (`internal/storage/assoc_last_activated_test.go`; RED-checked — the stale stamp moved the anchor back 400 days and one decay pass took the edge from 0.900000 to the 0.045000 floor). Clamping a *future* stamp down to now is the opposite direction, is not covered, and is named in the helper's doc comment.
  - **Dependency:** the mechanism is only as good as `lastActivated`. `UpdateAssocWeight`/`UpdateAssocWeightBatch` stamp it; `DecayAssocWeights` deliberately does not. Any future path that writes an association without stamping it becomes a silent "this edge never fades" bug.
  - **Named residual:** `lastActivated` is `int32` (2038 overflow). Post-2038 `Δt` goes negative, the guard clamps it to 0, and decay silently *stops* — fail-safe in direction, but it needs widening, which is a value-format change.
  - **Amendment (`fix/assoc-read-error-laundering`): the anchor may not be reset by a read that failed.** `peakWeight` and `createdAt` are re-encoded by every `UpdateAssocWeight`/`UpdateAssocWeightBatch`, from the values the metadata read reported. So a read that reports *absence* when it actually FAILED collapses the ceiling of a live edge on the next pass. The readers therefore distinguish three outcomes, not two: a missing key is absence `(zero, nil)`; a Pebble read failure is an error; and a **present but under-length fixed-width index record** (0x14, 0x1E — every writer stores exactly 4 bytes) is also an error, because reading it as absence makes the writer skip the delete of the live keys and persist a SECOND edge for the pair with a reset anchor. `UpdateAssocWeight` refuses the whole update on any of these; `UpdateAssocWeightBatch` skips only the affected pair, applies the rest, and reports `*storage.AssocBatchSkipError` (a corrupt block is a permanent, key-scoped fault on a writable DB, so aborting the batch would cost every other vault in the flush window its learning indefinitely). Pinned behaviourally by `TestAbsenceIsNotFailure` (`internal/storage/read_absence_vs_failure_test.go`) with a completeness census over the `pointGet` seam; the Hebbian worker fires `OnWeightUpdate` only for updates that persisted (`internal/cognitive/hebbian_callback_persistence_test.go`). **Named residual of the amendment:** a skipped pair is not stamped with `lastActivated`, so a pair that is actively co-activated but permanently unreadable is treated by `DecayAssocWeights` as unused and decays toward the floor while in constant use — the right trade against corrupting it, but a real one, and the reason the skip is now reported (the Hebbian worker folds an unreadable *weight* read into the error `processBatch` returns and logs the pair once, `internal/cognitive/hebbian_read_fault_test.go`). The same laundering in `FlagContradiction`, which is not behind the `pointGet` seam and has a worse consequence, is tracked separately as #804; the missing `iter.Error()` check in `associationsForOne`, which needs an iterator-level fault seam before it can be RED-proven, as #808.

- **[COG-28]** **If a query's evidence reaches ANY member of a DECLARED version chain with admission-worthy strength, recall resolves that evidence to the chain's authoritative current head before thresholding and ranking, and says so** — except where the chain forks or cycles, in which case recall refuses to choose and names the refusal. "Declared" means an explicit `storage.RelSupersedes` edge written by `Evolve`/`EvolveAt`, `Consolidate`, or `Link(relation=supersedes)`, and nothing else: this invariant consults no similarity, extends nothing from the heuristic currency layer, and adds no inferred supersession of any kind (COG-25 stays advisory-only, on both sides of any declared chain). Issue #763; `internal/engine/activation/shadow.go`, `internal/engine/engine_version_head.go`.

  **The bug it closes is an ORDERING bug.** `EvolveAt` soft-deletes its predecessor, but nothing removes that predecessor from HNSW or FTS — its vector and its postings are exactly the ones the user's OLD wording matches. Phase 6's lifecycle cut discards it before it is ever scored, so its earned relevance is thrown away instead of redirected, and `applySupersession` (which already injects an absent head for the VISIBLE stale case) is structurally unable to see it. When an evolve changes the vocabulary, a query phrased against the old wording reaches only the predecessor and the current truth is stored, current, and invisible.

  **Split by layer, each half reusing a proven mechanism.** Phase 6 keeps the evidence: a candidate refused by exactly one of the two currency predicates while carrying the declared-supersession signature (soft-deleted + a CLOSED `ValidUntil`, the evolve shape; or still-active with an elapsed closed `ValidUntil`, the `Link(supersedes)`/`forget(not_true_since)` shape) is scored by the SAME functions at the SAME `req.Threshold` against the SAME quantity the fusion mode already gates on (`AbsoluteScore` for ACT-R/CGDN, `Final` for weighted_sum) and emitted as an `activation.ShadowMatch`. `Activations` is byte-identical whether shadows exist or not. The engine layer resolves and injects, reusing `resolveSupersessionHead` verbatim (view-scoped head resolution, hidden-node traversal, fork refusal, cycle refusal, retracted-successor voiding) and the COG-22 gate.

  **Honesty rules, all mandatory.** The head ranks at the predecessor's `Final` (and `max(own, shadow.Final)` when it was already retrieved) — the existing "the score belongs to the TOPIC" doctrine, extended to the case where the stale member is invisible; re-scoring the head on its own absolute would gate the successor on wording it deliberately no longer has, reproducing the bug one layer deeper, and an ε-discount would silently say we trust the author's declaration less than we claim. `ScoreComponents` on an injected row are the PREDECESSOR's measurements, verbatim, and `substituted_for` + `substitution_basis` (+ the `Why` clause under `include_why`) are never optional — they are what keeps those numbers honest. `chain_truncated` reports a walk that ran the depth cap out; `head_not_indexed_yet` distinguishes "no embedding yet" from "not relevant".

  **The only admission path is a predecessor that cleared the caller's own threshold on the unchanged formula.** Substitution REDIRECTS admission-worthy evidence; it never CREATES it. Enforced structurally: shadows are scored in a SECOND pass over a disjoint candidate set, so they cannot enter `maxRaw`/`sigma`/`denom` (a hot superseded predecessor must not rescale the live result set), and the `inTagPool` bypass is explicitly NOT applied to them (a tag hit defines the returned SET; a shadow is evidence, and letting a filter manufacture a substitution would admit a head on zero aboutness). Capped at `shadowMatchCap = 16`, score-descending with a ULID tiebreak; nil-allocated on every query that produces none.

  **Named exclusions, each a decision.** No shadows are collected at all under `as_of`/`include_invalid` (under `as_of` the predecessor IS the right answer and the successor may not exist yet in the view), under RRF fusion (rank-based finals with a ~0.001 coerced default make "cleared the bar" carry almost no information — the same calibration reason COG-18's R1 amendment skips the entity boost; a named deferral), or under Explain's `threshold < 0` diagnostic bypass (at that bar every superseded predecessor "clears", so Explain would report a `would_return` recall never produces).

  **Named exceptions to the invariant's own statement.** A FORK (a hop with more than one active superseder) and a CYCLE both refuse — the existing WARN-and-abstain, preserved. A retracted successor voids the chain and the predecessor is never resurrected. When those leave the response empty, the abstention says which: `ambiguous_version` (fork wins over everything) or `superseded_only`, instead of the generic `below_threshold` — the difference between falling silent and a sentence an agent can act on. `TotalFound += injected`, with the same known double-count deviation the two existing injectors carry.

  **One behaviour change in the shared walker, and it is load-bearing.** `resolveSupersessionHead` now distinguishes a RETRACTED successor from one that was itself SUPERSEDED, using the same closed-`ValidUntil` signature: soft-deleted + closed stamp is a chain intermediate the walk passes through; soft-deleted with an OPEN stamp (a plain `muninn_forget`) or archived still voids the supersession at that hop for every caller. The discriminator is exact rather than heuristic — supersession soft-deletes AND stamps atomically, a plain forget leaves the stamp open, and `forget(not_true_since)` stamps without soft-deleting. One non-supersession sequence produces the same signature: `forget(not_true_since)` followed later by a plain `muninn_forget` (stamp closed first, soft-delete second). It is benign — with no `RelSupersedes` edge the walk never fires on such a node, and as a mid-chain node it is traversed-but-unnameable, yielding `no_current_version` rather than `retracted` (same no-substitution outcome, different reason string). Without it, EVERY multi-hop evolve chain was unresolvable (each intermediate an evolve leaves behind is soft-deleted, so the walk read the first one as "retracted" and voided the whole chain): A→B→C returned nothing at all, which is #763 again with an extra hop. It also now returns `Truncated` and a refusal `Reason`; the depth cap's truncation was silent, which was tolerable while the stale row stayed visible beside the head and is not when the injected head is the only row the caller sees. On an evolve chain PAST the cap no node within the cap is nameable, so recall abstains `superseded_only` rather than presenting an invisible intermediate as current.

  **Amendment (#779): `Consolidate` is a third declaring writer, and it is a JOIN.** Consolidation replaces N memories with one, which is the same content-replacing act evolve performs — but it archived its sources with a plain soft-delete (no edge, an OPEN `ValidUntil`), the exact signature this invariant reads as "trash, not history". Consolidation was therefore the ONE content-replacing operation excluded from the mechanism built to close this hole, and unlike the embed-lag race #779 reports (already closed — `Consolidate` calls `Engine.Write`, which fires `onWrite`, #767) that exclusion was permanent. It now writes the evolve shape per source: a `RelSupersedes` edge from the merged engram plus soft-delete + closed stamp, one atomic batch each. Two structural notes. **A join is not a fork**: many sources point at ONE merged engram, so each source still has exactly one superseder and `resolveSupersessionHead`'s ambiguity refusal (which is for RIVAL superseders at a hop) never fires on an ordinary merge. **The sources' FTS postings are now KEPT**, exactly as an evolved predecessor's are — deleting them (what the old `Forget` soft path did) is precisely what made the merged content unreachable from the lexical side. Sources are consequently time-travellable lineage under `as_of`/`include_invalid` where they were previously plain archived records; this is a deliberate, CHANGELOG'd contract change. **Consolidations that ran BEFORE this change are handled at read time, not migrated** (the #810/#854 precedent): their sources carry the plain-forget signature, which every read path already interprets correctly as "soft-deleted, no lineage" — identical to today's behaviour, no repair pass, and no rewrite of validity stamps at rest. Pinned by `internal/engine/engine_consolidate_lineage_test.go`.

  Pinned by `internal/engine/engine_version_head_test.go` (chain shapes, visibility parity, the non-substitution paths, the normalization-leakage detector, the adjacent-topic precision corpus), `internal/engine/engine_version_head_localassets_test.go` (the issue's own reproduction with the real bundled bge-small and a real HNSW, plus the unindexed-head case), `internal/engine/activation/shadow_test.go` (signature, request exclusions, tag bypass, structured filter, cap) and `internal/engine/activation/shadow_measure_test.go` (zero shadows across the 16 nonsense probes with a declared chain grafted into the abstention corpus). *A memory system that stores truth but cannot retrieve it under a natural phrasing is not dependable enough to drive autonomous decisions — and the fix must not buy that back with false positives, which is why the precision half is measured rather than argued.*


- **[COG-29]** **When two results in a recall response are joined by an UNRESOLVED DECLARED `contradicts` edge, recall must not present either as the answer.** Both are returned, DEMOTED below the score they earned, in a defensible order, with a first-class per-row annotation, and the response carries a `conflict` block naming the pair and the resolution actions. The phase (`internal/engine/engine_contradiction.go` `applyContradictionHonesty`, issue #764) is **DEMOTE-ONLY** — every score change is a `min()` against the row's own earned score, so a conflict can only ever push a row DOWN, never above a genuinely better unrelated match — **never removes a row** (`len(out) == len(in)`), **never abstains**, and **WRITES NOTHING** (observe-safe by construction, COG-11).

  **"Declared" means an explicit `storage.RelContradicts` edge on a 0x03/0x04 key, and nothing else.** No similarity-inferred conflict, ever — the same asserted/inferred boundary #758/#763 established and COG-25 restates. Deliberately keyed on the EDGES rather than the 0x0A marker: that marker's key is `…|id(16)` with a single-partner value, so one engram can record exactly one partner and a second contradiction overwrites the first (keyspace-registry 0x0A).

  **Unresolved** means all of: (1) the edge is declared; (2) BOTH endpoints are live — not soft-deleted, not archived, validity window not elapsed, and nameable under the caller's visibility gate; (3) no `RelSupersedes` exists between the pair in either direction ("I declared which one wins" IS a resolution — asserted beats asserted, COG-25's doctrine); (4) under `as_of`, the edge existed at the caller's instant (a zero `CreatedAt` is a legacy edge, treated as always-existing — never invent a time, and over-warn beats under-warn on a correctness signal). Clause 3 is isolated by `TestContradictionHonesty_SupersedesEdgeAloneResolves`, which writes the `RelSupersedes` edge STRAIGHT THROUGH THE STORE: the engine's `Link(supersedes)` also retires the target, so a test that goes through `Link` is cleared by clause 2 and passes with clause 3 deleted — it proves nothing about clause 3. Clause 2's rule is applied by both recall (`contradictionEndpointLive`) and the report (`markResolvedContradictions`, which currently inlines an equivalent retirement closure — same rule, two sites; collapsing them into the shared function is cheap hygiene whenever either is next touched), so recall and the report agree on whether a conflict is live. **It is the clause that makes resolution work at all**: before #764 NO operation in the product cleared a declared contradiction — evolve, forget (soft), forget(not_true_since), hard-delete and `muninn_decide` all left both the edge and the marker in place, so an agent that resolved a conflict the way the tool advises watched the theater continue forever, with a hard-deleted endpoint rendering a permanently blank-concept row. Note that "in the result set" is NOT proof of liveness: under `include_invalid` a retired endpoint is returned, and treating its presence as liveness keeps the theater running past the resolution.

  **Not top-level abstention**, which is what one round-7 evaluator literally asked for. #747/#754 is the hard-learned lesson in this exact area: suppressing BOTH sides destroys the true memory exactly as hard as the false one, and the agent that did the right thing loses both facts and the conflict itself. It also breaks the `Abstained` wire contract ("Empty iff Abstained is false") that two admission-worthy candidates do not satisfy. The complaint underneath the ask — "an agent commonly consumes only the first result, so this is confidently misleading" — is about presenting one side as THE ANSWER, not about returning the data; the demote, the always-on per-row annotation and the `conflict` block remove "the answer" while keeping both facts.

  **Named deviations from the design, each with its reason.** (a) **Ordering is by score, and only by score.** After the demote the whole response is RE-SORTED by score with a stable sort; adjacency is whatever that produces. Neither gather direction is used. The design asked for gather-to-highest, which would lift a cluster member above a strictly better unrelated row — exactly what demote-only forbids. The first implementation therefore gathered to the cluster's LOWEST-ranked member instead, and that is worse: on a corpus with a dominant conflicted row (0.859) whose declared partner is a weak straggler (0.018), it moved the best answer from rank 0 to rank 6, behind five rows it outscores by 2-8x, and returned a response that was no longer score-descending — an agent reading the first result then gets a 0.5 row presented as the answer over a 0.77 row, a worse lie than the one the phase exists to fix. Sorting by the demoted score is both demote-only AND monotone: near-tied rivals (the evaluators' shape) land adjacent for free, a dominant answer stays at rank 0 demoted and annotated, and nothing is ever lifted because a row's score only ever went down. The intra-cluster ladder (`sortContradictionCluster`) is applied first as a permutation within the cluster's own positions, so it decides sibling order on an exact score tie and nothing else. `keepAtLeast`/`adjacency_overflow` are computed from POST-SORT positions — the demote itself can push a member below rows that used to score under it, so computing against pre-sort ranks silently cuts a conflict in half while reporting that it did not (pinned by `TestContradictionHonesty_DominantAnswerStaysRankZero` and `TestContradictionHonesty_AdjacencyOverflowUsesPostSortRanks`). (b) The design's "if any cluster member survives the MaxResults cut, all of them do" does not hold at a tight limit: phase 6 truncates to `MaxResults` inside `activation.Run` BEFORE any post-pipeline phase, so a partner beyond the limit never reaches this phase, and injecting it is deferred (neither side is known to be right, so a conflict must never LIFT content into a result set it did not earn). What holds instead, and is pinned, is that the surviving row is still demoted and annotated with the partner named and `partner_in_results: false` — an agent consuming only the first result is still told it is disputed. The `keepAtLeast`/`adjacency_overflow` machinery still defends the case where a post-pipeline injector grows the set past `MaxResults`.

  **No absolute score ceiling.** An earlier draft carried a hard 0.95 cap and a `score_capped` wire field. Recall's `Score` is an absolute activation that is structurally an order of magnitude below that on every corpus measured, so the ceiling never once bound: it shipped a permanently-false flag on four surfaces and a test assertion that could not fail. Both were removed. The honest statement of the guarantee is the relative one — `contradictionDemote` (10%), a single `min()` against the row's own earned score.

  **The per-row annotation names exactly ONE partner, and the choice is total.** A row can be an endpoint of several declared edges; selection prefers a partner that is itself in the result set, then the lowest partner ULID. Taking the first edge encountered while ranging the map `GetAssociations` returns made both `with` and `partner_in_results` flip between identical queries against an unchanged vault (measured: a 33/7 split over 40 calls). Pinned by `TestContradictionHonesty_PartnerChoiceIsDeterministic`.

  **Fast path — three probes, cheapest first.** (1) A per-vault in-process flag set by `Engine.Link`, so a single-binary deployment honors a declaration on the very next query. (2) A bounded 0x0A seek (`PebbleStore.HasContradictionMarkers`, one iterator `First()`), the steady state and the only probe that sees an edge declared by ANOTHER process. (3) Once per vault per process, `DeclaredContradictions` — a bounded forward-association scan for declared-but-unflagged edges, its verdict memoised per vault.

  Probe 3 exists because 1 and 2 together had a PERMANENT hole. The in-process flag lives only in memory and the 0x0A marker is written only when the batch worker flushes, so a process that restarts between `muninn_link(contradicts)` and that flush loses the flag while the marker was never written — and nothing re-probed. Recall then silently stopped honoring a durable, correctly-declared contradiction FOREVER, which is the project's worst failure class dressed as a fast path. Pinned by `TestContradictionHonesty_SurvivesRestartBeforeTheMarkerFlush` (declare, assert no marker, clear the in-process state, recall must still honor) and `TestContradictionHonesty_ProbeIsPaidOncePerVault` (the memoisation, so recall never pays the scan twice). A probe ERROR — marker or declared-edge — degrades toward doing the work, never toward silently disabling the phase, and so does an incomplete scan (a vault with more forward associations than `DefaultDeclaredContradictionScanCap`).

  **Named residual:** an edge declared by another process/node AFTER this process memoised a clean verdict is honored only once its 0x0A marker lands (≤ one worker interval after the COG-23 #764 amendment) — probe 2 still runs on every query, so nothing regressed here.

  **`contradictionsDeclared` is never evicted, deliberately.** Eviction would have to prove no unresolved declared contradiction remains anywhere in the vault — a vault-wide scan, the exact cost this gate avoids — and getting it wrong turns honesty off silently. The entry is an empty struct keyed by an 8-byte prefix; the retained set is bounded by vault count. The false positive costs a read-only bounded phase that returns no block when nothing is live.

  Because the corpora contain no `contradicts` edge, the phase is a provable no-op on all three (`TestContradictionHonesty_NoContradictionsIsANoOp`), which is what keeps their numbers byte-identical. **Measured cost** (`BenchmarkRecallContradictionGate`, 200-engram synthetic vault, 1000 recalls x3): gate closed p50 348-383us / p99 1.03-1.32ms; gate OPEN p50 360-372us / p99 1.03-1.22ms — the open gate is inside the noise of the closed one. The once-per-process declared-edge scan is 8.5ms over 100k associations (`BenchmarkDeclaredContradictions`).

  Surface scope: MCP + MBP + REST (alias). gRPC and the non-Go SDKs carry NO annotation fields at all, so adding only this one would be the silently-wrong class — obligation #3, a deliberate omission. Pinned by `internal/engine/engine_contradiction_honesty_test.go`, `..._resolution_test.go` (every resolution path, RED-checked at `bb10f30`) and `internal/mcp/contradiction_honesty_test.go` (the two places an MCP field silently vanishes). **Accepted residual:** an agent that declares a conflict and walks away leaves both facts demoted 10% — visible, recoverable via three named actions the warning spells out, and strictly better than one of them being presented as the truth.

- **[COG-30]** **Every recall row carries an ABSOLUTE `relevance_band` (a WORD, never a float), and the phase that assigns it is a PURE ANNOTATION** (#773, `internal/engine/engine_relevance.go` `applyRelevanceBands` + `internal/engine/activation/relevance.go`). It runs LAST of the post-pipeline phases — after currency (COG-25), contradiction honesty (COG-29), truncation and the abstention recompute — and changes **no score, no order, no row membership, and writes nothing** (observe-safe, COG-11; pinned by `TestRelevanceBands_IsAPureAnnotation` and `TestRelevanceBands_EngineRowsMatchRawActivation`). The band is derived from `ScoreComponents.AbsoluteScore` against the vault's OWN resolved calibration — the COG-6 default gate and the resolved content-channel ceiling, **never `req.Threshold`** (a caller passing `threshold: 0.01` must not band a below-noise row `moderate`; pinned by `TestRelevanceBands_CallerThresholdIsNotAnInput`). Band edges are self-derived ratios of landed invariants (2x the gate = weak ceiling per COG-26's noise placement; half the content ceiling = strong floor per the measured saturation bound), never corpus-tuned constants (principle #11). Classification rules, each the product of a review round: **(a)** `filter_match` is decided by the REASON a row was admitted (`Admission`, carried from the scoring path; a COG-5 floored row is `AdmissionTagFilter` via `ScoreComponents.ContentMatchFloored`), never by threshold arithmetic — `tagMatchFloor` numerically equals the COG-6 ACT-R gate, so a fresh confidence-1.0 tag-only reminder lands EXACTLY ON the boundary and an arithmetic test banded it `weak` while its 0.9-confidence twin banded `filter_match` (G6 refute finding 1; pinned by `TestAdmissionOf`, `TestRawTagRange_TagOnlyAdmission_IndependentOfConfidenceAndRecency`, `TestRecall_TagOnlyHit_BandsFilterMatch_RegardlessOfConfidenceAndRecency`, all RED-checked at `dda8a3e`). **(b)** `uncalibrated` bases name the TRUE cause: an operator's explicit `semantic_floor: 0` reports `semantic_floor_disabled`, never `no_model_baseline` — same identity transform, opposite cause (G6 finding 3, RED-checked at `dda8a3e`); rrf/weighted_sum are response-wide uncalibrated (they do not gate on AbsoluteScore); post-pipeline injections with zero-value components are `not_scored` ("not measured" ≠ "measured and low"); a COG-28 substituted head is NOT `not_scored` — it bands on the predecessor's real measurements, which `substitution_basis` already attributes. **(c)** The response-level weak-band hint is DEFERRED under its own pre-committed rule (U(combined) 16.7% < 70%, A(moderate) 10% > 0% — the honest-negative record in `engine_relevance.go`, now drift-guarded by bounded assertions in `relevance_band_measure_test.go`). **Named conservative residual (cold start):** while a fresh write's embedding is still pending, the semantic channel is silent and a row's absolute score is capped at `w_fts` (0.4 default) — below the 0.5 strong floor — so `strong` is unreachable and genuine matches may under-band until indexing catches up; the error direction is deliberate (understate, never overstate). **Surface sync obligation:** the band and basis enums live on three wire surfaces (`internal/mcp/types.go`, `internal/transport/mbp/types.go`, `internal/transport/rest/openapi.yaml`) plus the guide's reading section (`internal/mcp/guide.go`); adding a band or basis value must land on ALL of them in the same change — gRPC and the non-Go SDKs deliberately carry no band (obligation #3's silently-wrong class).

- **[COG-31]** **Symmetry is a property of `storage.RelType`, and only the recall RANKING phases may read an edge from both endpoints.** `RelType.IsSymmetric()` (STRICT: `RelCoActivated` 0x0000 — the Hebbian relation, which until #800 was the only RelType with no name — plus `RelRelatesTo` and `RelContradicts`; everything else, **including `RelUserDefined`**, is directional) and `RelType.BidirectionalForRanking()` (`IsSymmetric` ∪ the ≥`0x8000` user-defined range, admitted under principle #4's fail-open-on-presentation) are the single source of truth — `internal/storage/types.go`. Two classes of edge are therefore read backwards during ranking without their author having declared them symmetric: the user-defined range above, and LEGACY BLANK-VALUED edges, which `decodeAssocValue` returns as relType 0 — indistinguishable from `RelCoActivated`, a collision that predates #800 but that #800 gave a second consequence (before it, relType 0 only fed `profile.AllowsEdge`; now it also decides reverse admissibility). Both are bounded by the same blast radius as everything else here — ranking and traversal only, no writer, no direction-presenting surface — so they can nudge an ordering and cannot manufacture a presented fact. Separating a real co-activation from a blank legacy value needs a value-format change and a migration. **`PebbleStore.GetAssociations` is FORWARD-ONLY by contract**, because its consumers include a WRITER (`consolidation/transitive.go` persists inferred edges) and direction-presenting surfaces (`Engine.Traverse`, REST `/associations`, `GetAnnotations`); unioning `0x04` into it made dream consolidation persist manufactured transitive facts and made REST report "the OLD version supersedes the NEW one" with a green suite. The union lives in `PebbleStore.GetRankingNeighbors`, whose only legitimate consumers are `phase4HebbianBoost` and `phase5Traverse` — **of which only the first emits anything today: #801 measured `phase5Traverse` as inert on every real corpus (its hop gate has sat above the phase's own score ceiling since the initial commit), so this invariant's user-visible effect is currently carried entirely by `phase4HebbianBoost`, and the `hop_path` clause below describes a response field that is always empty in practice. The traversal consumer and its pins are kept deliberately (decision record, #801); nothing here is relaxed by that, because a consumer that is revived later must land on a correct read** — it LOSES edge direction by construction and must never be consumed by a writer, or by a presenter OF EDGE DIRECTION. That last clause is narrower than it first reads and the distinction is load-bearing: `phase5Traverse`'s BFS output does reach a caller, as `mbp.ActivationItem.hop_path` — but a hop path is a list of engram IDs, an assertion that two memories are connected, not an assertion about which way any relation points; the traversed `RelType` is dropped before the response is built. `traversedCandidate.relType`/`scoringCandidate.relType` are therefore WRITE-ONLY today and nothing pins them unread — a future scorer reading either is reading a relation out of the union whose direction is gone (a `RelSupersedes` there may point either way), which is fine to rank on and forbidden to present or to derive a direction from; the fields say so at their declaration. What must never surface is a DIRECTION read out of this union. *Root cause: writers each picked a direction and picked different ones (Hebbian canonicalises older→newer, the neighbour/autoassoc workers write newer→older), so a forward-only read scored the same single relationship at full strength from one endpoint and exactly ZERO from the other.* Mechanics that are load-bearing, each pinned: **(a)** the reverse half is a SINGLE iterator over the 0x04 range with sorted forward seeks, not N `GetReverseAssociations` calls (BFS passes 20 ids per level); **(b)** the cap is a true top-N over the MERGED list via a two-pointer merge — both ranges are weight-descending — because concatenate-then-truncate fills the cap with floor-weight forward edges and discards a 2000x heavier reverse one (`TestGetRankingNeighbors_CapIsTopNOverMerge`); **(c)** dedup by `TargetID` BEFORE the cap, keeping the larger weight, or a pair written in both directions is counted twice by `phase4HebbianBoost`'s sum (`TestHebbianBoost_PairCountedOnce`); ties go to the forward stream so a reverse edge never displaces an equal-weight forward one. **The reverse adjacency is CACHED** (`revAssocCache`, same 500k/2s shape as `assocCache`, evicted on the DESTINATION at every site that evicts the forward cache on the source): measured, an uncached reverse half cost ~152µs against the cached forward half's ~11µs on a 50-candidate read and pushed whole-recall p50 15-20% over budget, because "one extra bounded iterator like the forward one" ignored that the forward one is served from cache. Its entries carry `truncated` so a later caller asking for a larger cap is re-scanned rather than silently under-served — a trap the forward cache still has (#820, which also covers the forward miss path's aliased return). **Every non-empty slice `GetRankingNeighbors` returns is freshly allocated** (the no-forward-edges exit returns `nil`, which owns nothing), and that is a property of the WHOLE function, not of one half: `rankingReverseEdges` copies on its hit AND its miss path (`TestRankingReverseEdges_MissPathDoesNotAliasTheCache` — the miss path used to return the slice it had just cached, and the symmetric shortcut `if len(fwd) == 0 { return rev }` in the merge would have published the cache's array to both ranking phases for the 2s TTL), and `mergeRankingNeighbors` copies on its `len(rev) == 0` shortcut too (`TestGetRankingNeighbors_NoReverseEdgesDoesNotAliasTheCache`), because that shortcut forwarded `GetAssociations`' own miss-path slice — which DOES alias `assocCache` (#820) — straight to the caller. Until that was fixed the union's return was copy-safe for a node WITH inbound symmetric edges and aliased for a node without, decided by data the caller cannot see; a non-uniform ownership contract is the trap, not the aliasing. `GetAssociations` still owes its other consumers the same guarantee (#820). Cost of the extra copy, measured with `BenchmarkPhase4Read_ForwardOnlyFan` — 50 candidates, `maxPerNode` 20, warm caches, forward edges only (the ONLY shape that reaches the copy with a non-empty list; `BenchmarkPhase4Read`'s ring gives every node inbound edges and its degree-0 arm returns nil, so neither of its arms performs the copy): median per 50-candidate call **+4.1µs at forward degree 2, +6.8µs at 10, +13.2µs at the cap of 20**, with allocations 62 → 112 (exactly one per candidate) and 85.2K → 162.0K B/op at the cap. The cost scales with the number of edges returned, so the cap is its ceiling. Against a whole-recall p50 of ~26 ms (embedder in the path) that is ~0.05%; against the ~0.5 ms no-embedder harness it is ~2.6%, and it is accepted at both because a non-uniform ownership contract is the defect. The fixture's shape is asserted in the gate by `TestForwardOnlyFanFixture_TakesTheMergeCopyShortcut` (every candidate has forward edges, none has inbound ones), so a later edit cannot quietly move the quoted number back onto an arm that does not perform the copy. **When the union read FAILS, the entire Hebbian term is dropped for that recall, loudly** (`slog.Warn`, one per recall, not per candidate) — never a fallback to the forward-only `GetAssociations`. That fallback preserves more absolute signal and reinstates this invariant's own failure mode while doing it: two candidates holding an identically-weighted symmetric edge to the same recent engram score `w` and `0` purely by writer-chosen orientation, and `hebbianBoost` multiplies the final RRF score (measured on the root-cause fixture: healthy 0.5/0.5, forward-only fallback 0.5/0.0, whole-signal drop 0.0/0.0). The trade is signal-preservation against tie-preservation, and ranking that loses a term uniformly beats ranking that keeps a biased half of it — the same reason an unreachable embed backend degrades to BM25-only rather than to a half-applied vector score. Pinned by `TestHebbianBoost_UnionReadFailurePreservesSymmetricOrder`, which asserts the relative ORDER of the two arms (every magnitude assertion on that path passed on the defective fallback) and that the WARN fires. **No new Pebble prefix, no value-format change, no migration: existing vaults are fixed retroactively the moment the read lands, because 0x04 has been fully maintained all along.** Pinned by `TestRelType_SymmetryCensus` (parses `types.go` with go/ast and fails on any declared-but-unclassified RelType, SEC-15 shape — it keys off the const BLOCK that declares `RelSupports` and classifies every name in it regardless of how it is typed, because a parser that required the `X RelType = 0x1` annotation stayed green on `X = RelType(0x1)`, and the person the census exists to catch is exactly the one who will not match the house style; plus any `RelType`-annotated constant elsewhere in `types.go`, parenthesised annotations unwrapped; guarded by `TestRelTypeConstantNames_CatchesUnannotatedDeclarations`. Its residual is wider than "unannotated" suggests and is stated form-by-form at `relTypeConstantNames`: a SECOND const block gets only the annotated sweep, so an `= RelType(0x31)`, an iota continuation or a bare `= 0x32` there escapes; an ALIAS-annotated member (`type RelKind = RelType; const RelAliased RelKind = 0x32`) escapes though it IS annotated, because go/ast alone cannot resolve the alias; and another FILE in the package is not parsed at all. The anchor check `t.Fatal`s if the `RelSupports` block stops existing, so the parser cannot silently census an empty set), `TestGetAssociations_StaysForwardOnly`, `TestGetRankingNeighbors_DirectionalExcluded`, `TestGetRankingNeighbors_ReverseCacheInvalidated`, `TestHebbianBoost_IsSymmetricInPairOrder` (exact equality to 1e-9; RED at `b272c74`: the older→newer arm scored `0.0103`, the newer→older arm `0`) and `TestRecall_HebbianBoostSymmetricEndToEnd` (RED: the same link scored `hebbian_boost` 0.4 from its source and 0 from its target, dropping that row's recall score 0.7150 → 0.3862).

- **[COG-32]** **`HebbianEnabled` is SYMMETRIC: it gates the read side too.** `internal/engine/activation.ActivateRequest.HebbianEnabled` gates phase 4 (`phase4HebbianBoost`) exactly the way `PASEnabled` gates phase 4.5, and `internal/engine.Activate` sets it from the vault's resolved `PlasticityConfig` — the same flag that already gated Hebbian LEARNING submission and association DECAY (COG-16). Before #797 the read-side boost was UNCONDITIONAL, so a vault with `hebbian_enabled:false` was still scored by association edges it would never update and never decay: a frozen prior nothing in the vault's configuration could turn off. **User-visible behaviour change:** the `scratchpad` preset (`hebbian_enabled:false`, `assoc_decay_factor:0`) now scores recall without any Hebbian contribution; rows can reorder on such vaults. **Direct activation callers get the zero value — i.e. OFF.** That is deliberate (a bool defaulting to "on" cannot express "off"), and it means an in-tree fixture that seeds `AssocLog().Record(...)` plus `store.assocs` must now say `HebbianEnabled: true` or its priming is inert; four did (`abstention_gate_test.go`, `abstention_gate_measure_test.go`, `recall_queryset_measure_test.go`, `engine_version_head_test.go`'s saturating arm) and were updated with the gate. Pinned by `TestPhase4Hebbian_RespectsHebbianEnabled` / `..._EnabledStillBoosts` (`internal/engine/activation/hebbian_gate_test.go`) and `TestActivateRequest_WiresHebbianEnabledFromPlasticity` / `..._HebbianEnabledVaultStillBoosts` (`internal/engine/hebbian_read_gate_test.go`), all four RED-checked at `857291f`. *This is the switch a KILL verdict on the cognitive layer would have to flip; before #797 turning Hebbian "off" did not turn the read side off, so the kill was not executable.*

  **Companion (same increment): an explicit `actr_heb_scale: 0` is honored, not substituted.** The scalar multiplies BOTH `hebbianBoost` and `transitionBoost` inside softplus, so 0 is the whole-prior KILL SWITCH — and `auth.PlasticityConfig.ACTRHebScale` has always been a `*float64` clamped to `[0, 50]`, i.e. the config layer always admitted it. TWO layers below then threw it away: `Engine.Activate`'s `if resolved.ACTRHebScale > 0` and `activation.resolveWeights`' `if req.ACTRHebScale > 0`, each silently restoring 4.0. Made unrepresentable rather than special-cased (principle #3): `activation.Weights.ACTRHebScale` is now a `*float32` — nil = unset, non-nil is honored exactly. The MBP/REST wire field stays `omitempty float32`, where 0 genuinely means absent, so the transport path maps 0 to nil and only the per-vault plasticity path can express a configured zero; that asymmetry is deliberate and commented at the call site. `ACTRDecay` was deliberately NOT converted: `auth` clamps it to `[0.01, 2.0]`, so an explicit 0 is not a representable configured value and its sentinel is unambiguous. Pinned by `TestResolveWeights_ExplicitZeroHebScale_NotSubstituted` (+ the unset/explicit controls) and `TestACTRHebScaleZero_KillsTheCognitiveBoost` / `..._Default_CognitiveBoostStillApplies`, RED-checked at `a8aaa46`.

- **[COG-33]** **A derived entity edge is reported only while a LIVE engram still supports it, and that support is decided at READ time.** `Engine.GetEntityAggregate` (`internal/engine/entity_ops.go`) filters both `relationships` (0x21) and `co_occurring` (0x24) against the set of entities that an engram passing the same lifecycle test as the response's own `engrams` field still co-mentions with the subject. Issue #780.

  **Why read-time and not a decrement at supersede time.** The 0x24 co-occurrence counter and the 0x1F mention count are MONOTONE capture-time ledgers: incremented on every write, every inline-entity write and every evolve carry, and decremented only by `DeleteEngram` — i.e. only by a HARD delete. `DecrementEntityCoOccurrence` does exist and does delete the key at zero (the issue's claim that it does not is false), but supersession is a soft-delete plus a stamp, so nothing funds it. A capture-time instruction therefore cannot fix the vaults that already have the skew, and **no user action short of destroying the record ever could** — the correction has to live where the value is read. Same reasoning as COG-28's amendment above, and the same #810/#854 precedent: reinterpret at read time rather than rewrite data at rest. The COUNT is deliberately left as the ledger reports it — it is a historical strength signal and is honest as one; PRESENCE is the thing that was lying.

  **The half that was never broken, and must stay unbroken.** A plain `muninn_evolve` CARRIES the predecessor's entity set onto the live successor and re-increments every pair, so after an ordinary evolve the pair genuinely has live support and reporting it is correct. The defect appears only when the caller REPLACES the entity set (`entities[]` on evolve — the "we moved off that technology" act) or the supporting engrams are otherwise superseded, leaving an edge whose entire support is retired while the same response's `engrams` list already excludes it — an aggregate contradicting itself.

  **Bounded, and it degrades to the old behaviour rather than to a wrong answer.** The live-support scan stops at `liveSupportScanCap` (500 live engrams; a work bound, not a calibration). Past the cap — or if a partner set cannot be read — the live set is a SUBSET of the truth, and filtering on a subset would DELETE real edges, which is strictly worse than reporting a stale one: the filter is abandoned entirely, everything is reported unfiltered as before, and a WARN says so. Pinned by `internal/engine/entity_live_support_test.go`.

## Storage & durability invariants

- **[STO-1]** A new Pebble prefix must be disjoint from the keyspace registry and added to it; the disjointness cross-check must cover it. The registry lives in `internal/prefix/prefix.go`: add the prefix to `prefix.All()` and the disjointness tests (`TestAll_NoDuplicateBytes`, `TestAll_OwnerGroupsPairwiseDisjoint` in `internal/prefix/prefix_test.go`) auto-tighten to cover it — there is **no `storageMaxPrefix` constant to bump** (removed with #618's registry consolidation). — see `keyspace-registry.md`.
- **[STO-2]** Any path mutating engram **state or lease** must hold `casLocks.For(id)` across read→commit, as `CompareAndSet` (`storage/lease.go`) and `DeleteEngram` (`storage/engram.go`) do. *A new unlocked state-mutating path reopens the #594 resurrection race.*
- **[STO-3]** CAS state changes go through `UpdateMetadata` (keeps 0x0B index, caches, provenance, replication consistent) — never a direct 0x02 write — `storage/lease.go`.
- **[STO-4]** The engram lease (0x2A) is **advisory**: recall/claim paths consult it; normal writes must not start consulting it without an RFC — `storage/lease.go`, `engine/engine_lease.go`.
- **[STO-5]** `DeleteEngram` deletes association keys from **live Pebble scans**, not the engram's inline ERF list (the Hebbian worker may have moved weights), and must include the 0x2A LeaseKey in its batch — `storage/engram.go`.
- **[STO-6]** `ClearVault` ordering is load-bearing: point-delete 0x15 count → evict in-memory counter → drain coalescer → drain provenance → tombstone `DeleteRange`. Any new vault-scoped prefix MUST be added to its `dataPrefixes` list — `storage/vault_lifecycle.go`.
- **[STO-7]** MOL WAL recovery hard-fails on CRC mismatch, never skip-and-continue; replay functions must stay idempotent (last-seq is written NoSync) — `internal/wal/mol.go` `Recover`.
- **[STO-8]** HNSW: entry-point promotion happens only after the node is linked; `LoadFromPebble` validates entry-point reachability via `bfsReachable` and rebuilds if disconnected; `Registry.Insert` rolls back the orphan vector if graph insertion fails — `internal/index/hnsw/` (guards PR #471's four defects).
- **[STO-9]** Vault dimension: all insert/search paths go through `CheckDim`; a failed graph load sets `loadErr` and refuses inserts (never reports dim 0 = "empty vault" and recreates a split embedding space) — `internal/index/hnsw/registry.go` (#582/#589).
- **[STO-10]** Idempotency receipts give **no** concurrency guarantee — `WriteIdempotency`/`CheckIdempotency` are unlocked Get/Set. A caller needing exactly-once under concurrency must add its own lock. A PR claiming idempotency is race-safe is wrong.
- **[STO-11]** **A prefix scan's exclusive upper bound comes from `keys.PrefixUpperBound`, never a naive last-byte increment.** Vault prefixes are 8 SipHash bytes, so roughly 1 vault name in 256 ends in `0xFF`, where `upper[len-1]++` wraps to `0x00` and produces an upper bound BELOW the lower bound: the iterator returns nothing, silently, forever, for that vault only. Three contradiction scans carried this bug (`HasContradictionMarkers`, `GetContradictionRecords`, `DeclaredContradictions`), which disabled COG-29 honesty outright for ~0.4% of vaults. Pinned by `TestContradictionScans_VaultPrefixEndingIn0xFF` (`internal/storage/contradiction_test.go`), which derives a `0xFF`-terminated vault name deterministically rather than hoping one turns up. **#800 addendum, closed by #819:** `keys.AssocRevRangeEnd` (the COG-31 reverse-range bound) delegates to `PrefixUpperBound` for this reason, and `keys.AssocFwdRangeEnd` now does too. The forward side used to open-code a carry loop that stopped at index 1 — correct for the ~1-in-256 case STO-11 describes, but an ALL-0xFF workspace prefix (2^-64) ran the loop off the end and produced `0x03|00..00`, an inverted bound. Pinned by `TestGetRankingNeighbors_VaultPrefixEndingIn0xFF` (reverse), `TestGetAssociations_VaultPrefixEndingIn0xFF` and `TestGetAssociations_AllFFWorkspacePrefixIsNotSilentlyEmpty` (forward), and `TestAssocRangeEnds_NeverInvertTheirBound` (both, at the unit level). **The bound must also not be LOOSE (#816).** `PrefixUpperBound` used to increment the first sub-`0xFF` byte from the right and return WITHOUT clearing the trailing `0xFF` bytes, so `03 aa ff` yielded `03 ab ff` and admitted all of `03 ab 00 .. 03 ab fe` — a different prefix's keyspace, for ~1 prefix in 256. It now carries and TRUNCATES. `storage.PrefixIterator` open-coded a byte-identical copy of the loose loop and now delegates to the helper, so there is one implementation. **`PrefixUpperBound` returns `nil` — meaning UNBOUNDED, which is what `pebble.IterOptions.UpperBound == nil` does — when no finite bound exists (an all-`0xFF` prefix, or the empty prefix).** The values it used to return there (`append(prefix, 0x00)`, `[]byte{0x01}`) were bounds that EXCLUDED almost every key carrying the prefix, i.e. the silently-empty scan this invariant exists to prevent. Unreachable in-tree — byte 0 of every scanned prefix is an allocated type prefix (highest `0x45`) — but callers on destructive paths should know that `nil` widens. Pinned by `TestPrefixUpperBound_IsTightAndNeverCrossesIntoANeighbouringPrefix` (`internal/storage/keys/scan_test.go`), whose load-bearing half is "no key from a DIFFERENT prefix is admitted".

- **[STO-12]** **An association edge is refused unless BOTH of its endpoints have a live 0x01 engram record** — `internal/storage/assoc_endpoints.go` — the 0x03 forward key, the 0x04 reverse key, the 0x14 weight index and the 0x25 archive row all inherit this (#803).

  *A refusal at every writer, NOT an unrepresentable state, and the wording is deliberate.* The first version of this entry claimed an edge "may only exist while" both endpoints are live, which asserts the principle-#3 structural impossibility that the mechanism does not deliver. `engramExists` reads through `ps.db.Get` and the write commits a separate batch, with nothing spanning the two — `DeleteEngram` holds `casLocks.For(id)` across its read→commit (STO-2), the association writers take no lock — so a hard delete that lands inside the check→commit window still leaves a row. **Measured residual: 6 dangling rows per 400 forced `DeleteEngram`-vs-`WriteAssociation` races (pre-guard: 400/400).** Closing it structurally was considered and rejected: `casLocks` on both endpoints would work for `WriteAssociation` alone, but `pebbleStoreBatch` holds an open batch across arbitrary CALLER code between its check and its commit, so no lock discipline the store can enforce covers it — the claim would still have to be qualified, at the price of N-lock ordering against a single-lock `DeleteEngram`. The residual is a repairable row and it belongs to the deferred O(vault) integrity/repair pass, not to a lock.

  *Liveness is EXISTENCE, not lifecycle state.* `SoftDelete` `batch.Set`s the 0x01 key with `StateSoftDeleted`, and archiving is a state on a present record, so a soft-deleted or archived engram **is present** and its edges must survive — a state-aware predicate here would silently destroy every restorable engram's edges, which is worse than the leak it closes (pinned by `TestSTO12_SoftDeletedAndArchivedEndpointsKeepTheirEdges`). Only `DeleteEngram` removes 0x01.

  **Two layers, both required.**

  *(1) The cascades.* `DeleteEngram` cascades 0x25 in both directions (it scanned 0x03/0x04 only), and every hard-delete CALLER must clean the FTS and HNSW indexes, which live outside the store — `Engine.Forget(hard)` and **both** `Engine.PruneVault` arms (MaxEngrams and RetentionDays are separate delete loops with separate call sites) do so via `cleanSearchIndexesAfterHardDelete`. The 0x03/0x04 cascade scans must also obey **STO-11**: a 0x03 key is `prefix(25)|weightComplement(4)|dst(16)` and `keys.WeightComplement` is `MaxUint32 - uint32(w*MaxUint32)`, so `complement[0] == 0xFF` for every weight at or below ~1/256 and the complement is `0xFFFFFFFF` at weight 0 — which is byte-for-byte the position every PRE-FIX weight-1.0 edge was written at (`legacyFullWeightComplement`). The original naive `append(prefix, 0xFF)` bound sorted at or below all of those, so the cascade silently skipped them and nothing else ever reaped them. **Client-reachable at ordinary weights** (nothing clamps `Weight` on `Engine.Link` or the inline path), and it made `RepairLegacyFullWeightAssocKeys` a *creator*: that pass scans with a correct bound, so it saw the stranded legacy row the cascade missed and relocated it into a live, correctly-positioned dangling edge at startup. Both bounds now use `keys.PrefixUpperBound`, and the repair's `flushChunk` live re-validation additionally requires both endpoints — needed independently, because the vaults that pass exists for were hard-deleted by a pre-fix binary whose cascade could not reach the legacy position either. Note the other direction too: `keys.PrefixUpperBound` WAS LOOSE — it incremented the first sub-0xFF byte from the right and returned WITHOUT clearing the trailing 0xFF bytes, so for a 25-byte assoc prefix ending in 0xFF the bound spanned into the neighbouring engram's association keyspace; `storage.PrefixIterator` open-coded the identical loop. **#816 fixed both** (the helper carries and truncates; `PrefixIterator` delegates to it). **Every destructive scan bounded by a 25-byte `kind|ws(8)|id(16)` prefix nevertheless KEEPS its explicit `bytes.Equal(k[:25], prefix)` break** — belt and braces now rather than the sole protection, because a delete loop should not depend on a helper in another package for the only thing holding it inside its own keyspace, and because any future non-ULID id tail collapses the ~2^-64 gap below to zero. There are four of them: `DeleteEngram`'s 0x03 forward and 0x04 reverse cascades, `DeleteEngram`'s 0x25 archived-source cascade, and `reapArchivedEdgesFrom` (reachable from the recall read path via `RestoreArchivedEdges`). The 0x03/0x04 pair must additionally keep `SeekGE` rather than being converted to `PrefixIterator` (whose First/Valid shape changes the break-vs-continue semantics on short keys). **There is a FIFTH scan over the identical 25-byte `0x25|ws|src` prefix that has NO such break, so its upper bound is its ONLY protection**: `RestoreArchivedEdges`' own candidate loop, twenty lines above `reapArchivedEdgesFrom` in the same file. It hand-rolls its own TIGHT upper bound (incrementing from the right and zeroing every byte it wraps) instead of calling `PrefixUpperBound` or `PrefixIterator`, which is why it was correct while those were loose. #816 makes the shared helper agree with it, and **the loop still stays hand-rolled**: "equivalent today" plus "the only thing standing between this loop and fabricated edges" is not a combination to maintain at a distance. Dropping that bound — by consolidating onto the shared helper and then regressing it, or by "tidying" this loop for consistency with its neighbour — would make it FABRICATE a `victim → neighbour's dst` edge that never existed and stamp `restoredAt` on it, permanently exempting it from `GCArchivedEdges`; this loop is destructive AND creative, unlike the four above. Its `engramExists(ws, c.dst)` guard does not catch that: the dst read out of the neighbour's key is a real live engram, so liveness and the bound are orthogonal. The bound is pinned by `TestSTO11_RestoreArchivedEdgesCandidateScanStaysInsideItsOwnPrefix`; a prefix check must not be folded into that loop's existing `len(k) < 41 || len(v) < 26` skip, because `len(v) < 26` is value-shaped and must stay a `continue` while a prefix mismatch must `break`.

  *Read the ~1-in-256 number correctly.* It is the rate at which the BOUND IS LOOSE, not the rate at which anything is lost — the two differ by about 64 bits. To land inside the widened band a second engram must share the victim's first 14 ID bytes: the entire 48-bit ULID millisecond timestamp AND 8 of the 10 crypto-random entropy bytes, i.e. ~2⁻⁶⁴ *on top of* a same-millisecond collision. With ULID-shaped keys that is **not operationally reachable**, and every test of it constructs its IDs by hand. The guard is therefore **structural hygiene, not a live data-loss fix**: a mandated shared helper whose contract is wrong, compensated by an explicit per-key check at each call site, where any future non-ULID ID tail (a counter, a truncated hash, a content-addressed key) collapses the 64-bit gap to zero immediately — silently, on a delete path. Pinned by `TestSTO11_EveryDestructivePrefixScanStaysInsideItsOwnPrefix` (the table over all four scans; grow it when a fifth appears), `TestSTO12_DeleteEngramCascadeReachesEveryWeightPosition`, `TestSTO12_DeleteEngramCascadeStaysInsideItsOwnPrefix`, `TestSTO12_LegacyFullWeightRepairNeverCreatesADanglingEdge` and `TestSTO12_HardDeletePathsCleanTheSearchIndexes`.

  *(2) The guard.* **Enumerate the writer set from the KEY BUILDERS, never from method names or from prose** — `grep -e AssocFwdKey -e AssocRevKey -e AssocWeightIndexKey -e ArchiveAssocKey` over non-test Go. The first version of this entry enumerated by name, claimed four writers, and missed four more with no "Association" in their signature at all: the inline-`Associations` loops in `PebbleStore.WriteEngram`, `PebbleStore.WriteEngramBatch` and `pebbleStoreBatch.WriteEngram`, plus the singular `UpdateAssocWeight`. **Those three inline loops are client-reachable** — `mbp.WriteRequest.Associations` arrives from REST (`rest.WriteRequest` is a type alias), gRPC, MBP and the embedded library, so an ordinary client holding an ID across a delete landed three dangling rows with a nil error; only MCP escaped, because `muninn_link`/`muninn_remember` use `relationships`, which routes through the guarded primitive. The full creator set, all refusing with `ErrDanglingEndpoint`, is: `WriteAssociation` (the primitive, through which `autoassoc.processJob`, `NeighborWorker`, `GoalLinkWorker`, the refines writer, `plugin/store_adapter.go` and inline caller *relationships* reach the keyspace), `pebbleStoreBatch.WriteAssociation`, `UpdateAssocWeight`, `UpdateAssocWeightBatch`, `RestoreArchivedEdges`, and the three inline-`Associations` engram writers. Non-creators the same grep returns, and why they are exempt, are recorded at the head of `assoc_endpoints_writers_test.go`. Guarding one amplifier site would be cosmetic: the next worker added would reintroduce the class. Pinned by `TestSTO12_EveryAssociationCreatorRefusesDeadEndpoints` (the enumerated table; grow it when the grep grows) and `TestSTO12_InlineAssociationsCannotNameADeadEngram` (the surface-reachable arm, asserted against the same census as the delete paths).

  *The engram writers check the TARGET only.* The engram carrying the associations is their source and its own 0x01 key is Set in the same uncommitted batch, so a both-endpoints DB read would reject every engram that has ever carried an inline association. `WriteEngramBatch` additionally treats any caller-supplied id elsewhere in the same call as live, ORDER-INDEPENDENTLY, because it sees the whole slice up front — but that promise is only sound if the sibling actually commits: the per-item validations that run afterwards (encode, the up-front raw-tag validation, STO-12 itself) each `continue` without queueing that item's 0x01, so the referrer's edges to a refused sibling are `batch.Delete`d after the loop. **Not fixed by accumulating the sibling set incrementally** — that reintroduces the queue-order dependence `TestSTO12_BatchAssociationGuardIsQueueOrderDependent` documents for `pebbleStoreBatch` and refuses the legitimate referrer-first ordering. **The predicate for that post-loop delete is `engramExists` PLUS the set of ids that another item in the same call commits, never the batch outcome.** Two distinct ways a refused item is still a live endpoint, and the delete must survive both: (a) a refused *update* leaves its 0x01 record in place, which `engramExists` sees; (b) the SAME id appears twice in one call, once committing and once refused, which `engramExists` CANNOT see — the successful occurrence's 0x01 `Set` is still in the uncommitted batch, so a DB read returns false and the referrer's edges to an endpoint that is about to commit are deleted in the same batch, after the `Set` that made it live. Hence the `committedIDs` set alongside the existence check. In both cases deleting is unrecoverable loss, where the hole being closed only leaks a repairable dangling row. Note also that two later `continue`s in the same loop (`WriteRawTagIndexEntry`, the 0x22 last-access `batch.Set`) fire *after* the item's 0x01 and association Sets are queued; both are unreachable today (the first re-runs the identical validation, the second fails only past a 4 GB batch), but a validation moved below the queueing would invert the repair. Pinned by `TestSTO12_BatchSiblingThatFailsItsOwnValidationIsNotALiveEndpoint`. **`pebbleStoreBatch` must additionally treat an engram queued EARLIER IN THE SAME BATCH as live** — `RememberChild` and `Evolve` queue the engram and its edge into one uncommitted batch, so a DB-read-only guard would reject every atomic child-append and every evolve.

  **The guard FAILS OPEN on a read fault — but counts and logs it** (`GuardReadFaults`, plus a WARN rate-limited to one per 30 s because a corrupt block is a permanent key-scoped fault). Fail-open is right and is *not* in tension with #809: #809 refuses on a read failure because proceeding would fabricate metadata over live data; this permits because refusing would destroy a live edge that can never be recovered — opposite loss directions, one doctrine. A silent fail-open would not be: it reverts the whole invariant to pre-#803 behaviour with nothing in the logs.

  *Ancillary obligations.* `DeleteEngram` also drops the 2 s-TTL `assocCache` entry of every source that pointed at the victim: the ROWS all go, but the SERVED graph kept naming the dead engram until expiry. `RestoreArchivedEdges` REAPS an unrestorable 0x25 prefix rather than restoring it: a restore stamps `restoredAt`, and `GCArchivedEdges` requires `restoredAt == 0`, so a restored-then-re-archived dangling edge would be permanently GC-exempt (`reapArchivedEdgesFrom`, pinned by `TestSTO12_ArchivedEdgesFromAHardDeletedSourceAreReaped`). Cost of the guard is one Pebble point read per endpoint, measured at 0.55–1.1 µs and independent of engram size (+1.0–1.6 µs on a 2.9 µs primitive; `assoc_endpoints_bench_test.go`). Behaviourally pinned end-to-end by `TestSTO12_NoDanglingAssociationEdges` (`internal/engine/engine_dangling_edges_test.go`), a table over the real delete/restore/repair sequences.

  *Three non-obvious facts measured rather than assumed:* `UpdateAssocWeightBatch` **does** create edges from nothing (#809 made a read FAILURE skip the pair, but read ABSENCE is a normal fact returning a nil error and a zero tuple, so a pair naming two engram-less ULIDs falls through both reads and Sets a fresh row, returning nil); `DecayAssocWeights` never reads 0x01, so nothing in the system ever reaped any of this — 24 passes with real `default` preset parameters deleted zero; and a client can steer an edge into the 0xFF complement band with nothing more exotic than `weight: 0.001`.

- **[STO-13]** **`engine.createdAtFloor` must stay strictly above the ERF zero-time sentinel instant, and the read-side "unset timestamp" test must be its exact complement.** `erf.decodeTimestamp` maps `erf.ZeroTimeSentinelNanos` — the bit pattern `uint64(time.Time{}.UnixNano())` overflows to — back to the zero time, so `IsZero()` works on records written before #810. That mapping is **aliased, not collision-free**: the sentinel is a legal value inside `UnixNano`'s defined range, so the instant `1754-08-30T22:43:41.128654848Z` encodes to exactly those bits and is **destroyed on read, silently, with no error**. The only thing making it unreachable is a validation constant in a different package — `createdAtFloor` (2000-01-01T00:00:00Z), which `validateCreatedAt` enforces on the caller-supplied path. Lowering it (the obvious change for a historical, genealogy or journal-import vault) makes the collision live. Derived from `storage.MinPlausibleTimestampYear`, which is also the single definition behind `storage.IsUnsetTimestamp`. The two are **exact complements**, not approximately aligned: `IsUnsetTimestamp` compares `t.UTC().Year()`, because `t.Year()` is evaluated in the value's own location and disagreed with the floor's absolute-instant `Before()` across a ~14-hour band on 2000-01-01 (cosmetic — 26-year-old timestamps — but the "in step" claim was false as written). Pinned by `TestCreatedAtFloor_IsAboveERFZeroTimeSentinel` (`internal/engine`), `TestDecodeTimestamp_SentinelInstantAliases` (`internal/storage/erf`) and `TestIsUnsetTimestamp_IsLocationIndependent` (`internal/storage`).

  **The set of guard sites is REGENERATED, never enumerated in prose.** `TestLastAccessElapsedCensus` (`internal/storage/lastaccess_census_test.go`) walks the AST of every non-test Go file in the module and fails on any elapsed-interval computation from a `LastAccess`-derived value — `time.Since(x)`, `y.Sub(x)`, `x.Sub(y)`, seen through intra-function taint (a local copy, and the ROOT of a field/index/map/pointer assignment target — `h.la = m.LastAccess` taints `h`) — that is not lexically preceded by an `IsUnsetTimestamp` call on the same value, or listed in `censusExemptions` with a structural reason. This exists because a grep-based enumeration of these sites **was wrong three times inside one PR**: it missed `computeComponents`, it counted the pre-existing copies as four when the merge base had two, and it named the dead `DecayWorker` as the sole unguarded path while `trigger.TriggerScore` — live, fed persisted metadata by the periodic sweep — had the identical shape and silently zeroed the whole `wRecency = 0.10` term on any cloned vault, so a subscription that fires on a healthy vault never fires on a clone (`TestTriggerScore_UnsetLastAccessDoesNotSilencePush`). Same shape as `TestAppendMode_MethodCensus` (SEC-15) — principle #6/#7. The census's honest boundary is stated in its own doc comment: no inter-function taint (including taint stored into a value that outlives the function), and lexical precedence is not dominance. **Its vacuity guard cannot be a count.** Two live sites reach the sink through a bare `.LastAccess` selector with no dataflow at all (`DecayWorker.processBatch`, and the `working.Manager.GC` exemption), so deleting the taint analysis outright leaves the census reporting PASS on 1 site instead of 6 — measured, not argued. The matcher is therefore pinned by its own fixture-driven unit test, `TestLastAccessCensusMatcherSeesLaunderedCopies`, and a claim that a selector-only matcher would "report zero and look green" is false.

  **A matcher self-check and an input-set check are different properties, and the census needs both.** The fixture test drives the matcher; it does not drive the `filepath.WalkDir` that FEEDS it. Adding three directory names to the walk's skip list (`activation`, `trigger`, `mcp`) dropped **four of the six sites and left BOTH tests green**, reporting "2 guarded site(s)" as a PASS — "found no unguarded sites" and "found no files" are indistinguishable outputs. `censusKnownSites` is the floor: six `file:func` pairs asserted present AND guarded, named rather than counted, so a walk change that loses one has to say which. Two further matcher blind spots were found by injecting probes into a real package rather than by reasoning about the boundary list — a sink inside a **package-level func literal** (the walk visited only `*ast.FuncDecl`), and elapsed time computed in **integer nanoseconds** (`nowNs - m.LastAccess.UnixNano()`), which was outside the stated boundary list entirely despite being the idiom `mbp.ActivationItem.LastAccess`'s int64 field already forces. Both are now covered.

  **"Over-tainting can only produce a false alarm, never a false all-clear" was FALSE**, and an overstated invariant is itself a defect. The same tainted set fed the guard side, so tainting the ROOT of an assignment target (`h.la = m.LastAccess` taints all of `h`) made an `IsUnsetTimestamp(h.created)` — a guard on an unrelated field — silence a genuinely unguarded sink. The taint analysis now returns roots (over-approximating, drives SINKS, where a false alarm is the worst case) and exact assignment PATHS (drives GUARDS, where it is not). Reproduced by `censusMatcherFixture`'s `guardOnADifferentFieldOfATaintedRoot`.

  *Related and deliberately NOT an invariant: `normalizeEngramTimes` governs only writers that CREATE timestamps. The six read-modify-write encoders rewrite a pre-existing sentinel back verbatim, so a vault cloned before #810 never self-heals — all six are exercised by `TestReadModifyWriteWriters_PerpetuateSentinel_DoNotHeal` (`rmwWriters`), and that is the reason the decode-side repair cannot be retired. **`TouchAccess` is the only path that repairs a record**; `UpdateMetadata` is a pass-through and three of its four callers (dedup, `Restore`, `CompareAndSet`) hand it a decoded `LastAccess` and round-trip the sentinel straight back (`TestUpdateMetadata_HealsOnlyWhenTheCallerSuppliesALastAccess`).*

  **A clone carries the source's `LastAccess`; only the counter resets.** `AccessCount` has a meaningful zero and recovers on first read; `LastAccess` has NO on-disk representation for "never" — that absence *is* this invariant — so any value the clone invents is a false statement about *when*, it never recovers, and it is re-consumed on every recall. The first fix wrote `LastAccess := CreatedAt`, which is correct for a newly created engram and wrong for a copy of an old one: #682's `ReinforceOnRead` (default true) keeps `LastAccess` fresh on memories created years ago, and rewinding it to `CreatedAt` produces the same large `daysSince` #810 named as the bug. Measured on a one-recall cold-cache clone: **DEFAULT ACT-R breaks at 7 days of source age, `weighted_sum` at ~45, `rrf` is unaffected at 400** — so the DEFAULT mode is worse and breaks six times sooner than the `weighted_sum` symptom #810 was filed under. `normalizeEngramTimes` still runs on the clone path, supplying `CreatedAt` only when the SOURCE's own `LastAccess` is unset, which is how a vault cloned before #810 heals as it is re-cloned instead of passing the sentinel on. Pinned by `TestClone_AgedActivelyUsedSource_RecallEquivalent` (age × fusion sweep, with a never-accessed control that separates this defect from the pre-existing decay of an aged unread memory) and `TestCloneVaultData_LastAccessSurvivesOnDisk` / `TestCloneVaultData_SentinelSourceHealsToCreatedAt` (raw bytes).

  **Open residual (pre-existing), and the half of it that is now closed:** `mbp.ActivationItem.LastAccess` is stamped `eng.LastAccess.UnixNano()`, and `time.Time{}.UnixNano()` *is* the sentinel — so the decode repair is invisible through that round trip and the int64 `last_access` still carries `1754-08-30T22:43:41Z` to **REST, gRPC and MBP**, which inherit it by type alias, plus `openapi.yaml` and the SDKs. That is a genuine four-surface change and stays **OPEN**. The MCP half is **FIXED**: `mcp.Memory.LastAccess` is a `*time.Time`, omitted when the value is not a real instant (`mcp/convert.go` `knownLastAccess`), because that field is declared in `internal/mcp` and referenced nowhere outside it — so the "nullable wire field on four transports at once (obligation #3)" reason this was first filed under was wrong about the field an agent actually reads, and weighing a hypothetical cross-surface divergence above an actual self-contradiction inside one response row (staleness omitted as unknown, `last_access` asserted as 1754) was the wrong trade. Pinned by `TestHandleRecall_UnknownLastAccess_OmitsLastAccess`.

  > **Number collision, resolved (#823).** `STO-12` was claimed by three in-flight changes: this one, `fix/803`, and PR #730 from an outside contributor. The recommendation on record was that `fix/803` keeps `STO-12`; it merged first as #828, so this invariant is **STO-13**. Anything written before that merge which says "STO-12" and means the ERF zero-time sentinel means STO-13 — including the commit message of this branch's own first increment, which is left unrewritten deliberately.

- **[STO-14]** **No key `internal/replication` writes may live under `prefix.Idempotency` (0x19), and the log-entry sub-range must contain nothing but log entries.** The whole replication keyspace is `prefix.Replication` (0x2F) with a second discriminator byte — entries at `0x2F 0x01 | seq_be64(8)`, metadata at `0x2F 0x02 | name` — built exclusively by `internal/replication/keys.go`. This is a **structural** property (principle #3), not a policy: `ReplicationLog.Prune` range-deletes `[0x2F 0x01|be64(from), 0x2F 0x01|be64(untilSeq+1))` — `from` starts at 1 and advances to the previous call's watermark on every subsequent call, so a periodic prune that lands on an already-pruned point is a fast no-op instead of a redundant delete — and both bounds carry the entry discriminator, so no bound derived from a sequence number can address a metadata key or anything outside the prefix. #737 replaced #726's decode-per-key scan (which existed only to survive the collision this invariant now forbids by construction) with this plain range delete.

  *What it replaced (#726).* Replication inlined a raw 0x19 for every key, so a log entry (`0x19|seq_be64(8)`) and an idempotency receipt (`0x19|siphash(op_id)(8)`) were the same prefix, the same length, and in the same database — and the prune's `DeleteRange(0x19|be64(1), 0x19|be64(untilSeq+1))` was exactly "delete every receipt whose SipHash is below the watermark". On a real store that was 116,248 entries and 23 receipts sharing one keyspace. The seq counter was worse-placed still: `0x19|0xFF*8` is *inside* the entry range, at sequence MaxUint64. Losing it restarts the log at 1 and re-issues sequence numbers a follower has already applied, which `Applier.Apply` skips as "already applied" — silent divergence, not a visible failure.

  *The general rule the incident produced:* **a prefix must never mix fixed-width HASH-keyed records with fixed-width SEQUENCE-keyed records.** They are indistinguishable by shape, so a range delete over one is a range delete over the other, and the probability of a hit grows linearly with the sequence.

  *Migration and downgrade.* Existing stores are moved by migration v5 (`internal/storage/migrate/v5_replication_prefix_relocate.go`): metadata is relocated value-preserving; the legacy log entries are DELETED, key by key, behind a conjunctive positive identification (`IsLegacyReplicationLogEntry` — legacy key shape, does not parse as a receipt, msgpack-decodes as an entry whose `Seq` equals the sequence in its own key), never with a `DeleteRange`. Entries are dropped rather than copied because copying ~76 GB at startup is not a migration anyone can run, and because dropping them is precisely what a prune does — a Lobe behind the retained window rejoins by snapshot, which `Prune`'s own doc comment already names as the expected consequence. A downgrade is refused structurally by `Runner.Run`'s refuse-newer guard (stored 5 > a pre-v5 binary's `MaxRegisteredVersion` 4), which matters here more than usual: an older binary would find no seq counter and restart the log at 1. Pinned by `TestReplicationKeys_NeverCollideWithIdempotency`, `TestPrune_LeavesIdempotencyReceiptsAlone`, `TestMigrationV5KeysMatchReplicationPackage` (the migration's hard-coded destinations against the live constructors — the migrate package cannot import replication, so the two copies are bridged by test rather than left to drift), `TestMigrationV5RecognisesRealAppendOutput`, and `TestRunner_RefusesDowngradeAfterV5`.

  *Corollary — what a Cortex ships (#826).* A Lobe must never end up holding the Cortex's log. Two mechanisms put it there and both are closed: `SnapshotSender.streamChunks` iterated the whole database (`skipFromSnapshot` now drops the entry sub-range and ONLY that — the metadata, above all the seq counter, must still ship or a promoted Lobe restarts numbering at 1), and `RepLogAppend` was wired unconditionally on every cluster node, so a Lobe logged a full-size entry for each of its own local writes. `replication.LocalAppendFunc` suppresses the append on a node that is *definitively* a follower and **fails open on `RoleUnknown`** — the startup window — because a leader that stopped logging there would silently drop writes out of the stream its followers read. Neither filter is expressible under 0x19: it would also have dropped every idempotency receipt. Pinned by `TestSnapshot_DoesNotShipTheCortexLogToALobe`, `TestLocalAppendFunc_FollowerDoesNotGrowALogNobodyReads`, and `TestReplicatedBatchesCarryNoReplicationOrIdempotencyKeys` (`internal/storage`) — the last of which records the honest negative that the applier path, which #826 named as the mechanism, never carried a replication key at all.

- **[STO-15]** **A forward/reverse association write is refused when it would silently replace an existing edge of a DIFFERENT `RelType` at the same `(src, weight, dst)` key** — `internal/storage/assoc_reltype_guard.go`. The 0x03/0x04 keys are `ws|src|weightComplement|dst`; `RelType` lives only in the VALUE, so two edges between the same pair at the same weight collide on one key, and the second write used to overwrite the first with `ok:true` and no signal at all (#771). `link(contradicts, w=1.0)` followed by `link(supersedes, w=1.0)` between the same pair destroyed the contradiction declaration instead of resolving it — and the collision applies to any two distinct RelTypes sharing a weight, not just that pair. `checkRelTypeCollision` reads the existing value at the exact key first: a missing key, a same-RelType re-write (an idempotent confidence bump), or a read fault are NOT collisions and proceed; only a decoded RelType that DIFFERS from the write's own refuses with `ErrAssocRelTypeCollision`. This is the guard half of #771's stated fix shape, not the key-format half: **RelType is still not in the key**, so two distinct RelTypes still cannot coexist at one weight — a caller wanting that must pick a different weight or delete the existing edge first. Putting RelType in the key would be an on-disk format change requiring a migration (see `internal/storage/migrate/`) and is deliberately deferred; the #764 conflict-corpus tests already work around today's collision by using distinct weights, which is exactly the workaround this guard now enforces instead of silently tolerating its absence. Applies identically to `PebbleStore.WriteAssociation` and `pebbleStoreBatch.WriteAssociation` (both build the same 0x03/0x04 keys); the batch path checks against committed DB state only, matching the STO-12 endpoint guard's scope on that path. Pinned by `TestWriteAssociation_RelTypeCollisionRefused`, `TestWriteAssociation_SameRelTypeSameWeightIsIdempotent`, `TestWriteAssociation_DifferentWeightNoCollision`.

- **[STO-16]** **`DigestFlagsKey` (0x11) values decode-tolerantly across two formats, and a legacy 1-byte record's `DigestEmbedFailed` bit is reinterpreted as BOTH `DigestEmbedFailed` and `DigestEnrichFailed` on read, never rewritten in place.** Before #605, `DigestEmbedFailed` and `DigestEnrichFailed` were literally the same bit (0x80) in a fully-allocated `uint8`, so a transient embed-only failure also skipped that engram from the ENRICH retroactive pass, and vice versa. The fix widens the flags space to `uint16` and gives `DigestEnrichFailed` its own bit (0x100), stored as a 2-byte value the first time `SetDigestFlag` touches a record — `decodeDigestFlags`/`encodeDigestFlags` in `internal/storage/plugin_store.go`. Every already-persisted 1-byte record predates the split and cannot say which stage actually failed, so there is deliberately **no repair-pass migration**: `decodeDigestFlags` reinterprets a legacy 0x80 as both bits set, which reproduces the exact skip-both behavior the old aliasing already produced — no previously-flagged engram becomes newly stuck (regression) or newly eligible (silently different) purely from upgrading. Only failures recorded AFTER this fix are distinguished. `internal/plugin.PluginStore`'s flag/skipFlags parameters are `uint16` for this reason; the constants in `internal/plugin/types.go` are deliberately untyped so a caller matching by name never needs a cast. `internal/storage` cannot import `internal/plugin` (the dependency runs the other way), so `plugin_store.go` carries its own mirrored `legacyDigestEmbedFailed`/`legacyDigestEnrichFailed` constants for the decode — keep them numerically identical to `plugin.DigestEmbedFailed`/`plugin.DigestEnrichFailed` if either ever changes. Pinned by `TestDigestEmbedAndEnrichFailedAreDistinctBits`, `TestEmbedFailureDoesNotBlockEnrichScan` (`internal/plugin/`).

- **[STO-17]** **Every entity-graph key is vault-scoped, and the entity record (0x1F) is read and written only through a workspace prefix** — `internal/storage/entity.go`, `internal/storage/keys/keys.go`. `EntityKey(ws, nameHash)` is `0x1F|ws(8)|nameHash(8)`; `UpsertEntityRecord`, `GetEntityRecord`, `IncrementEntityMentionCount` and `DecrementEntityMentionCount` all take `ws`, and so does `Engine.SetEntityState`. Before #683 the record alone was keyed globally while the 0x20 forward link, the 0x23 reverse index and the 0x26 relationship index all carried the vault, which produced two live defects at once: `mention_count` was the sum of every vault's upserts (and entity IDF divided that cross-vault numerator by a single-vault denominator), and a lookup by name from a vault with NO links to the entity still returned another tenant's name, type, confidence, `first_seen` and count with an empty engram list — an existence-and-metadata oracle over another tenant's entity vocabulary, with `SetEntityState` the same hole in the write direction. `ListEntities` was never affected, because it enumerates through `ScanVaultEntityNames` over the vault-scoped 0x20 index; the leak was on the by-name lookups (`muninn_entity`, `muninn_entity_timeline`) and on the lifecycle write. Two consequences that are easy to get wrong: **0x23 keeps the vault in the key's MIDDLE** (`0x23|nameHash|ws|id`), so any scan that must stay inside one tenant needs `keys.EntityReverseIndexVaultPrefix` and not `EntityReverseIndexPrefix` — the orphan check in `DecrementEntityMentionCount` is exactly such a scan, and an unbounded one would keep a vault's record alive because a DIFFERENT vault still mentions the name; and 0x1F is now a member of `clearVaultDataPrefixes`, so `ClearVault` range-deletes it instead of walking links and decrementing counts. On-disk format change: migration v6 (`internal/storage/migrate/v6_entity_vault_scope.go`) relocates legacy records and is protected downgrade-wise by `Runner.Run`'s refuse-newer guard. Pinned by `TestEntityRecord_VaultsDoNotShareARecord`, `TestEntityRecord_UnknownInAVaultThatNeverSawIt`, `TestDecrementEntityMentionCount_OrphanCheckIsVaultScoped`, `TestEntityRecord_NoCrossVaultPhantom`, `TestSetEntityState_CannotReachAnotherVault`, `TestClearVaultDataPrefixes_Scope`.

  **Residual — the migration's merge-tombstone clause is a fan-out, not per-vault (found in review, not fixed).** `resolveMergedTombstones` (in `v6_entity_vault_scope.go`) gives a merge tombstone the FULL vault set of its `MergedInto` target, and that set can legitimately have more than one member — any vault that ever upserted, referenced, or was independently migrated toward the same target name. A vault with ZERO references to the merged-away name therefore receives a full tombstone record (`state="merged"`, `merged_into=<target>`) purely because some OTHER vault performed the merge — the same read-oracle shape STO-17 otherwise closes, reopened for exactly this one record shape. This is deliberate, not an oversight: it is NOT a regression (pre-migration, that vault saw the identical record through the old global key, so the migration PRESERVES an existing exposure rather than creating a new one), and there is no vault-attribution information anywhere in the keyspace — no key records WHICH vault ran `MergeEntity` — so there is nothing to derive a correct single-vault assignment from; dropping the tombstone for any vault in the fan-out whose target has more than one vault would silently discard a redirect a legitimate vault still needs, trading a bounded read exposure for actual data loss. The exposure is bounded: `ListEntities` cannot enumerate it (it walks 0x20, and the planted tombstone carries no forward link for the uninvolved vault), so it is reachable only by an exact-name lookup. It is also durable rather than self-healing: `UpsertEntityRecord` preserves `State`/`MergedInto` once `State=="merged"`, so if the uninvolved vault later legitimately acquires that name, its record stays pinned as a tombstone pointing at another tenant's canonical name, and a subsequent `MergeEntity` call on it fails with "already merged" until manually cleared. Pinned (as an intentional invariant, not a regression trap) by `TestV6_TombstoneFansOutIntoUninvolvedVault`.

- **[STO-18]** **A path that REPLACES an engram's tag set must delete the removed tags' index entries in the SAME batch it writes the new ones** — both the 0x0C tag index (`keys.TagIndexKey`) and the 0x2C raw-tag range index (`keys.RawTagRangeKey`) — `storage/engram.go` `UpdateTagsLocked`. The removal set is computed BEFORE `eng.Tags` is overwritten (the pre-read hands back the L1 cache's live entry, so a post-write read yields the NEW tags and deletes nothing), and it is diffed on the **derived key bytes**, never on the tag strings: both keys hash to 4 bytes (`keys.Hash` — 0x0C over the whole tag, 0x2C over the tag KEY only), so a string diff can delete an entry a colliding surviving tag still needs, while a key diff degrades a collision into a retained orphan — the pre-existing, safe direction. `RawTagIndexKeyFor` is the single shared derivation for both the delete and the diff. *Orphans cannot produce a false POSITIVE — phase 6's `passesMetaFilter` re-checks the real tag on the engram — but these indexes seed candidates against a BOUNDED budget, so every orphan burns a slot a genuinely matching engram needed: a false negative at the pool boundary, and it concentrates on `due:`-style tags, which are exactly the workload `muninn_update_tags` and `muninn_guide` recommend and which get retagged repeatedly.* Pinned by `TestUpdateTags_RetiresStaleTagIndexEntries` (`internal/engine/engine_update_tags_review_test.go`), RED-checked: five rolls of a `due:` tag leave 6 raw-tag entries for one engram without the removal pass, 1 with it. A new tag-derived index prefix must be added to this removal pass, not just to the write path (#720).

- **[STO-19]** **An archived (0x25) edge of a `RelType.IsSymmetric()` relation is stored under both its endpoints, and a restore of either row retires both in the same batch.** Before #806, `DecayAssocWeights` archived an edge only as `ws|src|dst`, and neither the Bloom filter (`AddToArchiveBloom`) nor a reverse index existed for the other side — `phase4_75ArchiveRestore` calls `RestoreArchivedEdges` per recall CANDIDATE, so an archived edge was restorable only when recall happened to touch the endpoint that was its `Src`, and because the Hebbian worker's `canonicalPair` always makes the OLDER engram `Src`, that meant the newer of a co-activated pair — exactly the memory a recent session is most likely to touch — could never trigger its own edge's restoration. The fix reuses `RestoreArchivedEdges`' existing src-prefix scan rather than adding a reverse index or key format: `DecayAssocWeights` writes a second row at `ws|dst|src` with the byte-identical archive value (same batch, same `AddToArchiveBloom` treatment) whenever `e.relType.IsSymmetric()` — the exact predicate COG-31 names as "safe for a WRITER... to consult" — so scanning from EITHER endpoint's own prefix finds an entry. A directional edge (including `RelUserDefined`) is never mirrored: restoring it from the non-authored endpoint would MINT a live 0x03/0x04/0x14 row asserting a direction its writer never declared, which is exactly what COG-31 forbids of a writer. `RestoreArchivedEdges` deletes both the row it scanned and its sibling (`c.relType.IsSymmetric()`) in the SAME commit as the live write, so a restored edge does not leave a stale duplicate sitting in the archive with `restoredAt` still 0 — `GCArchivedEdges`' restoredAt==0 criterion was written assuming one row per edge. No new Pebble prefix, no value-format change, no migration: `DeleteEngram`'s existing 0x25 cascade already scans the whole vault range for the deleted engram as EITHER key position ([`engram.go`'s "As target" scan]), so it already reaped mirror rows correctly before this fix had a reason to create them, and `RebuildArchiveBloom`'s existing scan already reads `k[9:25]` as "srcID" for every row, so it Blooms a mirror row's dst-as-key-position-1 for free. Downgrade-safe by construction: an old binary's restore logic does not know to delete a sibling, so a downgrade leaves a stale mirror row behind for a restored edge — a harmless, self-correcting duplicate (the next restore attempt on it just re-writes the same live values) rather than a new failure mode, and an old binary's archiving no longer mirrors newly-archived edges, silently reverting to the pre-fix asymmetry for data written during the downgrade window. Pinned by `TestArchiveRestore_SymmetricEdgeRestorableFromEitherEndpoint` (RED before the fix: destination-side restore returned zero edges), `TestArchiveRestore_DirectionalEdgeNotMirrored`, `TestArchiveRestore_RestoreDeletesBothArchiveRows`, `TestArchiveRestore_ConcurrentRestoreAndGC` (`internal/storage/archive_restore_test.go`).

- **[STO-20]** **`GetEngram`, `GetEngrams` and `L1Cache.Get` return the SHARED L1-cache `*Engram`, which is READ-ONLY; a writer takes `(*Engram).Clone()` first.** `internal/storage/engram_clone.go`, `internal/storage/cache.go`. The shared return is deliberate and stays that way — the L1 cache exists so a hot read costs a map lookup rather than a decode, and recall reads hundreds of engrams per query through `GetEngrams`. Copy-on-READ was measured and rejected: `BenchmarkGetEngramWarm` puts an L1 hit at 113-132 ns/op / 80 B / 2 allocs bare, 247-273 ns/op / 560 B / 5 allocs with a deep clone (~2.1x on the product's hottest read path), and a SHALLOW copy-on-read is cheaper but does not close the class at all, since `Tags`/`KeyPoints`/`Associations`/`Embedding` stay aliased and element mutation still races. So the copy is paid by the seven writers instead of by every reader. *Mutating the returned struct in place is a data race against every concurrent recall AND publishes UNCOMMITTED state — the mutation is visible to readers the instant the assignment runs, before validation and long before the batch commits, which is how a rejected tag set reached `muninn_read` and filtered its own engram out of a recall (STO-18's history).* **A per-engram stripe lock is NOT a fix and four call sites documented that it was:** `casLocks` serialises writers against each other, and recall's readers never take it (`engram.go` says so at `UpdateTagsLocked`'s re-cache), so `SoftDelete`, `UpdateTagsLocked`, `UpdateConfidence` and `UpdateConfidenceWithContradiction` each raced a reader goroutine doing nothing but `GetEngram` in a loop — demonstrated under `-race` on the first and third before the fix. `cache.Delete`-before-`GetEngram` does not help either: `GetEngram` RE-CACHES what it decodes, so the pointer it hands back is shared again by the time the caller sees it. The seven fixed writers are `pebbleStoreBatch.mutateEngram` (#858's reported race, behind `SupersedeEngram`/`UpdateEngramState`), those four, `UpdateDigest` (the enrichment path — no lock at all, and it aliased the caller's `keyPoints` slice into the cached struct), and `Engine.Restore` (which mutated the cached struct purely to shape its return value, then returned the cache's own pointer). *#492 fixed this same class in dream dedup and guarded it with a doc comment on `L1Cache.Get`; that comment is accurate, is still there, and did not prevent any of these seven.* The guard is therefore mechanical: `TestCachedEngramMutationCensus` walks every non-test Go file in the module and fails on any field or element write through a `GetEngram`/`GetEngrams`/cache-`Get` value not released by `x = x.Clone()` earlier in the same function. Because its steady state is ZERO offenders, it cannot check itself — "found nothing" and "went blind" are the same green — so it carries the two checks `TestLastAccessElapsedCensus` had to grow: `TestCachedEngramCensusMatcher`, a fixture-driven unit test of the matcher (17 shapes, including `eng.Tags[0] =`, `eng.Field++`, the `for _, e := range engs` shape #492 came out of, and a clone of the WRONG engram not counting as a release), and `cachedEngramKnownSources`, a named floor of the seven functions the walk must still SEE binding a cache read. Behaviourally pinned by `TestReadModifyWriteWriters_DoNotMutateTheCachedPointer`, driven over the SAME `rmwWriters` table as STO-19-era `TestReadModifyWriteWriters_PerpetuateSentinel_DoNotHeal` so a new read-modify-write writer inherits the check, plus `TestMutateEngram_DoesNotMutateCachedPointer` and `TestMutateEngram_ConcurrentSupersedeSameID` (the #858 reproduction: RED reported three `WARNING: DATA RACE` at `batch.go` `oldState := eng.State` against `eng.State = StateSoftDeleted`). Stated residuals, all in the census doc: inter-FUNCTION taint is invisible, a bare `cp := *eng` is treated as safe (true for field writes, false for element writes), and a new slice-valued field on `Engram` that `Clone` forgets to copy is not detected by anything.

## Security & transport invariants

- **[SEC-1]** A `cap_` token must never set `IsAPIKey` — exactly one of `IsAPIKey`/`IsCapability` is set — `internal/mcp/context.go`, `internal/mcp/types.go`. *This is what makes the recursion guard structural.*
- **[SEC-2]** Any privileged MCP tool (one that mints/grants/creates) gates on `IsAPIKey && Mode==ModeFull && <opt-in env>` before dispatch — `internal/mcp/server.go` (`muninn_create_workflow_vault` is the template). *New privileged tools must copy this triple gate.*
- **[SEC-3]** `ValidateAPIKey` accepts only the `mk_` prefix; REST/gRPC/MBP call **only** `ValidateAPIKey`, never `ValidateCapability` — `internal/auth/keys_store.go`, `middleware.go`, `grpc/server.go`, `mbp/server.go`. *Keeps cap_ MCP-only, structurally.*
- **[SEC-4]** Capabilities carry a non-nil TTL at both mint (`GenerateCapability` rejects nil) and validate (nil/past expiry = expired) — `internal/auth/capability_store.go`. *No immortal credentials.*
- **[SEC-5]** Invalid `mk_`/`cap_` on MCP fails closed — never falls through to static-token or open-server auth — `internal/mcp/context.go`.
- **[SEC-6]** Every registered MCP tool is in exactly one of `mutatingTools`/`readOnlyTools`, and its bucket matches a per-tool decision a human recorded in `expectedToolClassification`; unknown tools blocked for observe AND write modes; unknown key modes rejected — `internal/mcp/context.go`, `server.go` (census in `internal/mcp/tool_classification_test.go`). *A tool with no classification silently escapes mode enforcement; a tool with the WRONG one hands an observe credential a write (#731). Coverage is not correctness — nothing yet derives a bucket from what the handler calls. Residual: the buckets are complementary, so a tool cannot be denied to observe AND write (#822). See the decision record.*
- **[SEC-7]** A vault-pinned credential rejects any differing `vault` argument and never echoes the pinned name — `internal/mcp/context.go` (also REST/gRPC/MBP). *No cross-vault action, no name leak.*
- **[SEC-8]** Unconfigured vaults are locked (fail-closed, `Public:false`) on REST/gRPC/MBP — `internal/auth/vault_config.go`.
- **[SEC-9]** Toolset filtering is **advertisement-only**; dispatch never consults it; unknown values fail **open to full** — `internal/mcp/toolset.go`. *Presentation preference, not a security boundary.*
- **[SEC-10]** SSE per-POST: both a cached `cap_` and a cached `mk_` session credential are re-validated (live key-store read) before dispatch, so a revoked or expired credential cannot keep dispatching on an open SSE session — `internal/mcp/server.go`. *The cap_ half closed the #612 confused-deputy; the mk_ half closed the symmetric gap in #617 (was #615). A PR touching the SSE POST path must not reopen either.*
- **[SEC-11]** Vault names are `[a-z0-9_-]{1,64}` everywhere via `IsValidVaultName` — `internal/auth/token.go`.
- **[SEC-12]** Static-token compares are constant-time with a length cap; keys/caps are looked up by SHA-256(secret) — raw secrets are never stored — `internal/auth/token.go`, `keys_store.go`, `capability_store.go`.
- **[SEC-13]** **In cluster mode, a client-originated write is accepted ONLY on the Cortex, and every mutation it makes is replicated.** Closed by #596/#631 claim 3 (was open, and was exactly those bugs). Enforced by one `WriteGate` function, installed by `cmd/muninn` only when cluster mode is enabled and wired to `ClusterCoordinator.IsLeader()`. It is held in FOUR places, deliberately: `Engine.refuseWrite`/`refuseNonLeaderWrite` (transport-agnostic backstop — the embedded Go library and any future transport inherit it), `auth.Store.refuseNonLeaderWrite` (vault config/plasticity, `mk_` keys, `cap_` tokens, admin records — which bypass `PebbleStore` entirely), and REST + MCP at the request boundary, which exist only so those two surfaces can answer with a precise status and the leader hint (REST 421 Misdirected Request + `X-Muninn-Cortex-Id`/`-Addr` + code 4015; MCP JSON-RPC -32002). gRPC maps the engine error to `FailedPrecondition` in a server interceptor; MBP frames it as error code 4015. REJECT, not forward — the argument (no node-to-node write RPC exists, forwarding amplifies split-brain, rejection adds no wait, and the join path already redirects this way) is recorded at `mbp.NotLeaderError`. **Completeness is census-derived, not a list**: `TestSingleWriter_RefusesEveryWriteOnFollower` runs `guardedOps` ∪ `additiveWriteOps` — the two write buckets `TestAppendMode_MethodCensus` (SEC-15) forces every exported `*Engine` method into — against a follower gate, and `TestSingleWriter_AdditiveCensusIsComplete` pins the additive half to `appendAdditive`. Configuration replication is the other half: `auth.Store.SetReplicator` puts every config/credential write into the same replication log as engrams (`internal/auth/cluster.go`), which it never entered before — a full join snapshot carried config because it iterates the whole DB, the incremental stream did not, so a failover served the pre-failover defaults. **Residuals, stated:** (a) the gate is evaluated once at the top of an operation, so a node demoted between the check and the commit still commits that one write — closing it needs `replication.ValidateFencingToken` to be called on the write path, and it has NO production caller today; (b) BACKGROUND workers that mutate replicated state on a follower (pruner, decay, archive GC) are NOT gated — they have no caller to mislead and `WipeForResnapshot` discards their output, but they are still divergence and remain open; (c) the REST route decoration is an explicit list, not method-based, because a method-based rule would also refuse `POST /api/admin/cluster/failover`, `/cluster/enable` and `/replication/promote` — the routes an operator needs ON a Lobe to recover from a lost Cortex. A write route missing from that list still cannot diverge (the engine/auth gates catch it); it just reports the refusal as a generic error instead of a 421.
- **[SEC-14]** The elevated trust level `verified` (human-confirmed / admin-certified) can only be set on a write by a caller whose ctx `auth.ContextMode` is `full` or `write` — an `observe` credential is rejected with `ErrInvalidRequest`, never silently downgraded — `internal/engine/engine.go` `resolveTrust`. The gate is enforced in the engine (`Write`/`WriteBatch`), so it is transport-agnostic *only if every transport populates `ContextMode`*: REST (`internal/auth/middleware.go`), gRPC (`grpc/server.go`), MBP (`mbp/server.go` HELLO, from the key's mode / full for open sessions), and MCP (`mcp/server.go` `dispatchToolCall`, full for authorized mode-less sessions) all do. A transport that reaches `Write` without setting `ContextMode` would *over-block* (reject a legitimate full write) — that was the S8 integration bug on MBP + default-config MCP; fixed and regression-tested (`mbp/trust_mode_test.go`, `mcp/trust_write_wiring_test.go`, `engine/trust_write_test.go`). **Recorded scope decision:** "write/full credential" (D4) is deliberately the bar — so an anonymous writer to a `Public` REST vault (mapped to `ModeFull` by design) and a `cap_`/`mk_` token minted with `full`/`write` mode *can* stamp `verified`. `verified` therefore asserts "written by a full/write-authorized principal," not "cryptographically human-attested"; a stricter human-only tier would need its own mechanism and is out of scope. *Every other trust level (`inferred` default, `external`, `untrusted`) is ungated. `source_type` is provenance-derived, never a write arg, so trust is the sole caller-settable discriminator — a write path that lets an observe credential stamp `verified` makes the anti-pollution capture tiering theater (S8/D4).*
- **[SEC-15]** An `append`-mode credential (`auth.ModeAppend`) may read and CREATE new engrams (`remember`/`remember_batch`/`remember_tree`/`add_child`) but is refused every operation that modifies or deletes an EXISTING engram/entity/lease/enrichment. Enforced at the ENGINE via `Engine.refuseWrite` — which calls `refuseNonLeaderWrite` (SEC-13) and then `refuseAppend` (returns `ErrAppendForbidden`) — on ALL 30 destructive methods (data-plane + vault-admin), so the guarantee is transport-agnostic — the MCP `isAdditiveTool` dispatch gate alone left REST/gRPC/MBP open (the #687 confirmed break; Restore/Link/PruneVault were later-found stragglers). `TestAppendMode_MethodCensus` reflects EVERY exported *Engine method and fails CI on any UNCLASSIFIED one, forcing each new method into guarded/additive/infra/read-only — real completeness, not a hand-maintained checklist (principle #6, cf. SEC-6). `TestAppendMode_RefusesEveryDestructiveOp` behaviorally proves all 30 guarded ops refuse append. *One recorded residual: the additive path may reinforce an existing engram's access metadata via content-hash-dedup `TouchAccess` (#682) — append strengthens, never overwrites/evolves/forgets/archives/re-trusts. This is the structural credential the flush write reflex uses so untrusted transcript content can never turn capture into destruction (cf. SEC-7 for the cross-vault half).*

- **[SEC-16]** **A `ClusterCoordinator` is constructed only at boot, from the same configuration decision that wires `storage.PebbleStoreConfig.RepLogAppend`** — `cmd/muninn/server.go`. The store's replication hook is captured when `NewPebbleStore` runs and only when `cluster.yaml` already said `Enabled` at that moment; there is no setter for it afterwards, and adding one would not be enough (an in-flight write would race the install, and every write that landed before it would be absent from the log while the node presents a complete-looking replica to joiners). `POST /api/admin/cluster/enable` therefore persists the configuration and answers **`202 Accepted` with `restart_required: true`** rather than starting anything — a coordinator built mid-process reports itself enabled, accepts Lobes, ships them a snapshot, and replicates none of the node's subsequent writes; it also never receives `SetMOL`, so it never prunes. That was #628, and it is why the runtime factory seam was **deleted** rather than completed: the bad state is now unrepresentable, not merely discouraged (principle #3). *Reintroducing any path that builds a coordinator outside boot reopens it.* Pinned by `TestClusterEnable_RequiresRestartAndBuildsNoCoordinator` and `TestEnableClusterRuntime_NeverStartsACoordinator` (`internal/transport/rest/`). Note the pre-enablement history is covered by the join-time snapshot, not by the log: `JoinHandler.HandleJoinRequest` sets `NeedsSnapshot` for every joiner and quotes `SnapshotSeq = repLog.CurrentSeq()`, and the snapshot is a full Pebble snapshot — so a replica that joins after a restart-enabled node comes up is complete even though the log starts empty.

- **[SEC-17]** **A bulk transfer's liveness bound is progress-based, never a fixed deadline on the whole transfer** — `internal/replication/peer_conn.go` (`progressWriter`). A single constant applied to a variable-size frame is a threshold measured against one quantity and applied to another: the frame may be a 40-byte heartbeat or a 1 MB snapshot chunk, and the link may be loopback or a WAN tunnel. The pre-#627 5 s per-frame deadline made replica catch-up **self-reinforcing** — a replica applying more slowly than the primary pushed filled the socket buffer, one write crossed 5 s, the stream died, and it rejoined further behind. The write deadline now renews on every byte the peer accepts, so "slow" and "wedged" are different conditions, and **raising the constant is not an acceptable substitute for this shape**. Three bounds keep it finite and must all stay: `sendIdleTimeout` (no bytes accepted at all ⇒ dead), `sendMaxDuration` (a dribbling peer may not hold one connection's write slot forever), and `sendSlotWait` (a caller that cannot get the write slot gets `ErrPeerBusy` — **the peer is alive, the connection must NOT be torn down**, so the shared MSP broadcast tick never blocks behind a multi-megabyte transfer and the streamer retries rather than dying). *A `Send` error path that treats `ErrPeerBusy` as a connection failure re-creates the spiral from the other end.* Pinned by `TestPeerConn_Send_SurvivesSlowButProgressingPeer`, `TestPeerConn_Send_WedgedPeerStillFails`, `TestPeerConn_Send_OuterBoundStopsDribblingPeer`, `TestPeerConn_ControlSendDoesNotBlockBehindBulkWrite`. Related: a restarted stream resumes from the highest position the primary can prove the replica holds (`ClusterCoordinator.streamStartSeq` — last ack, or the join-time `SnapshotSeq`, whichever is higher), never from 0; resuming from 0 made the re-transfer volume track the log size instead of the lag.

---

## Known open wounds

Several of these invariants sit next to known-imperfect code. The individual issues are
tracked publicly (#596, #605, #607, …); a reviewer should recognize when a PR is touching
one and either fix or at least not widen it. Cite the individual issue rather than building
a one-stop list here — a consolidated weakness map is more useful to an attacker than to a
reviewer, who is only ever looking at one diff.

Found something that isn't tracked? See [`SECURITY.md`](../../.github/SECURITY.md) — report
it privately rather than opening a public issue.
